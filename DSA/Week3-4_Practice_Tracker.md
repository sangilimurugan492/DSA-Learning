# 🎯 DSA Guided Practice Tracker — Week 3 & 4

> **Purpose:** Practice problems you've already solved — with progressive hints so you never get fully stuck.
> **Goal:** Wean off looking at solutions. Go from `📖 Read` → `💡 Hints` → `✅ From scratch` → `🏆 Mastered`
> **Problems:** 36 (Week 3: 18 Stack + Simulation | Week 4: 18 Linked List + Array Review)
> **Scope:** Week 3 & 4 only. Trackers for later weeks will be created as you progress.

---

## 📖 How to Use This File

### The 4-Day Re-solve Cycle (per problem)

| Day | What to Do | Mark |
|-----|-----------|------|
| **Day 1** | Read the solution, understand it fully. Close the file. | 📖 |
| **Day 2** | Try to solve using only **Hint 1** (pattern name). If stuck, reveal Hint 2. | 💡 |
| **Day 3** | Try to solve using only **Hint 2** (key insight). If stuck, reveal Hint 3. | 💡 |
| **Day 4** | Try to solve **from scratch** — no hints. | ✅ or 🔄 |

- **3 successful from-scratch solves** = 🏆 **Mastered**
- If you fail from scratch, go back to Day 2 and repeat the cycle

### Daily Practice Routine (30 min)
1. Pick **2 problems** from your "not yet mastered" list
2. Follow the cycle for each
3. Update the tracker table below

---

## 📅 Date-Based Preparation Schedule

> Each day maps to the DSA practice slot in your daily checklist. Follow this schedule to stay on track.

### Week 3 — Sep 3 (Thu) to Sep 9 (Wed)

| Date | Day | Problems to Practice | Pattern Focus | Time |
|------|-----|---------------------|---------------|------|
| **Sep 3 (Thu)** | Day 15 | #1 Valid Parentheses, #2 Min Stack | Stack (Basic) | 30 min |
| **Sep 4 (Fri)** | Day 16 | #3 Evaluate RPN, #4 Daily Temperatures | Stack (Monotonic) | 30 min |
| **Sep 5 (Sat)** | Day 17 | #5 Next Greater Element I, #6 Next Greater Element II, #7 Online Stock Span, #8 Min Parentheses to Remove | Monotonic Stack | 1.5 hr |
| **Sep 6 (Sun)** | Day 18 | #12 Add Binary, #13 Add Strings, #14 FizzBuzz, #15 Add Digits | Simulation (Basic) | 1.5 hr |
| **Sep 7 (Mon)** | Day 19 | #16 Spiral Matrix, #17 Spiral Matrix II | Simulation (Matrix) | 30 min |
| **Sep 8 (Tue)** | Day 20 | #18 Text Justification, #9 Longest Valid Parentheses | Hard Problems | 30 min |
| **Sep 9 (Wed)** | Day 21 | #10 Largest Rectangle in Histogram, #11 Car Fleet, + 2 review picks | Hard + Review | 1 hr |

### Week 4 — Sep 10 (Thu) to Sep 16 (Wed)

| Date | Day | Problems to Practice | Pattern Focus | Time |
|------|-----|---------------------|---------------|------|
| **Sep 10 (Thu)** | Day 22 | #19 Reverse Linked List, #20 Linked List Cycle | Linked List (Basic) | 30 min |
| **Sep 11 (Fri)** | Day 23 | #21 Merge Two Sorted Lists, #22 Remove Nth from End | Linked List (Two Pointer) | 30 min |
| **Sep 12 (Sat)** | Day 24 | #23 Middle of Linked List, #24 Palindrome Linked List, #25 Valid Sudoku, #27 Two Sum II (review) | Linked List + Array Review | 1.5 hr |
| **Sep 13 (Sun)** | Day 25 | #30 Merge Intervals, #31 Meeting Rooms, #34 Rotate Image, #35 Spiral Matrix (review) | Greedy + Matrix | 1.5 hr |
| **Sep 14 (Mon)** | Day 26 | #19 Reverse Linked List (review), #26 Contains Duplicates (review) | Review | 30 min |
| **Sep 15 (Tue)** | Day 27 | #28 Valid Palindrome, #29 Is Subsequence | Two Pointer | 30 min |
| **Sep 16 (Wed)** | Day 28 | #32 Non-Overlapping Intervals, #33 Gas Station (review), #22 Remove Nth (review), #36 Game of Life | Greedy + Matrix + Review | 1 hr |

---

## 📊 Master Tracker

| # | Problem | Pattern | Difficulty | Attempt 1 | Attempt 2 | Attempt 3 | Status |
|---|---------|---------|-----------|-----------|-----------|-----------|--------|
| 1 | Valid Parentheses | Stack (HashMap) | 🟢 | 3/9       | | | [x]    |
| 2 | Min Stack | Stack (Auxiliary) | 🟡 | 3/9       | | | [x]    |
| 3 | Evaluate Reverse Polish Notation | Stack (Postfix) | 🟡 |           | | | ⬜      |
| 4 | Daily Temperatures | Monotonic Stack | 🟡 |           | | | ⬜      |
| 5 | Next Greater Element I | Monotonic Stack | 🟢 |           | | | ⬜      |
| 6 | Next Greater Element II | Monotonic Stack (Circular) | 🟡 |           | | | ⬜      |
| 7 | Online Stock Span | Monotonic Stack (Span) | 🟡 |           | | | ⬜      |
| 8 | Minimum Parentheses to Remove | Stack (Marking) | 🟡 |           | | | ⬜      |
| 9 | Longest Valid Parentheses | Stack (Index) | 🔴 |           | | | ⬜      |
| 10 | Largest Rectangle in Histogram | Monotonic Stack | 🔴 |           | | | ⬜      |
| 11 | Car Fleet | Monotonic Stack (Sort) | 🟡 |           | | | ⬜      |
| 12 | Add Binary | Simulation (Carry) | 🟢 |           | | | ⬜      |
| 13 | Add Strings | Simulation (Carry) | 🟢 |           | | | ⬜      |
| 14 | FizzBuzz | Simulation (Modulo) | 🟢 |           | | | ⬜      |
| 15 | Add Digits | Simulation (Digital Root) | 🟢 |           | | | ⬜      |
| 16 | Spiral Matrix | Simulation (Boundaries) | 🟡 |           | | | ⬜      |
| 17 | Spiral Matrix II | Simulation (Boundaries) | 🟡 |           | | | ⬜      |
| 18 | Text Justification | Simulation (Greedy) | 🔴 |           | | | ⬜      |
| 19 | Reverse Linked List | Linked List (Iterative) | 🟢 |           | | | ⬜      |
| 20 | Linked List Cycle | Fast/Slow Pointer | 🟢 |           | | | ⬜      |
| 21 | Merge Two Sorted Lists | Linked List (Dummy) | 🟢 |           | | | ⬜      |
| 22 | Remove Nth from End | Two Pointer (Gap N) | 🟡 |           | | | ⬜      |
| 23 | Middle of Linked List | Fast/Slow Pointer | 🟢 |           | | | ⬜      |
| 24 | Palindrome Linked List | Fast/Slow + Reverse | 🟡 |           | | | ⬜      |
| 25 | Valid Sudoku | HashSet Lookup | 🟡 |           | | | ⬜      |
| 26 | Contains Duplicates (review) | HashSet Lookup | 🟢 |           | | | ⬜      |
| 27 | Two Sum II (review) | Two Pointer | 🟡 |           | | | ⬜      |
| 28 | Valid Palindrome | Two Pointer | 🟢 |           | | | ⬜      |
| 29 | Is Subsequence | Two Pointer | 🟢 |           | | | ⬜      |
| 30 | Merge Intervals | Greedy (Sort + Merge) | 🟡 |           | | | ⬜      |
| 31 | Meeting Rooms | Greedy (Sort) | 🟡 |           | | | ⬜      |
| 32 | Non-Overlapping Intervals | Greedy (Sort by End) | 🟡 |           | | | ⬜      |
| 33 | Gas Station (review) | Greedy | 🟡 |           | | | ⬜      |
| 34 | Rotate Image | Matrix (Transpose + Reverse) | 🟡 |           | | | ⬜      |
| 35 | Spiral Matrix (review) | Matrix (Boundaries) | 🟡 |           | | | ⬜      |
| 36 | Game of Life | Matrix (In-Place) | 🟡 |           | | | ⬜      |

**Legend:** ⬜ Not started | 📖 Read solution | 💡 Used hints | ✅ From scratch | 🏆 Mastered

---

## 🧩 Problems with Progressive Hints

### Pattern 1: Stack — Basic

---

#### 1. Valid Parentheses — 🟢 Easy
**LeetCode #20** | Solution: [ValidParentheses.kt](stack/valid_parentheses/ValidParentheses.kt)

- **Hint 1:** Pattern → Stack with HashMap
- **Hint 2:** Map open→close brackets. Push open brackets. On close, pop and check match. Stack must be empty at end.
- **Hint 3:**
  1. Map: '(' → ')', '{' → '}', '[' → ']'
  2. For each char: if open → push. If close → pop and check map[popped] == char
  3. Return stack.isEmpty()

**Blank Template:**
```kotlin
fun isValid(s: String): Boolean {
    val map = mapOf('(' to ')', '{' to '}', '[' to ']')
    val stack = java.util.Stack<Char>()
    // TODO: Push open, pop on close, check match
    return false
}
```

---

#### 2. Min Stack — 🟡 Medium
**LeetCode #155** | Solution: [MinStack.kt](stack/min_stack/MinStack.kt)

- **Hint 1:** Pattern → Auxiliary Stack (store pairs)
- **Hint 2:** Store (value, currentMin) pairs. Each entry remembers the minimum at the time it was pushed.
- **Hint 3:**
  1. Stack stores Pair(value, minAtThisPoint)
  2. push: min = if empty then value else minOf(value, top.min)
  3. getMin: return top.second

**Blank Template:**
```kotlin
class MinStack {
    private val stack = ArrayDeque<Pair<Int, Int>>()
    fun push(value: Int) {
        // TODO: Store (value, min(value, currentMin))
    }
    fun pop() { /* TODO */ }
    fun top(): Int { /* TODO */ return 0 }
    fun getMin(): Int { /* TODO */ return 0 }
}
```

---

#### 3. Evaluate Reverse Polish Notation — 🟡 Medium
**LeetCode #150** | Solution: [EvaluateReversePolishNotation.kt](stack/evaluate_reverse_polish_notation/EvaluateReversePolishNotation.kt)

- **Hint 1:** Pattern → Stack (Postfix Evaluation)
- **Hint 2:** Push numbers. On operator, pop two operands (b first, then a), compute a op b, push result.
- **Hint 3:**
  1. For each token: if operator → pop b, pop a, push (a op b)
  2. Else → push token.toInt()
  3. Return stack.last()

**Blank Template:**
```kotlin
fun evalRPM(tokens: Array<String>): Int {
    val stack = ArrayDeque<Int>()
    // TODO: Push numbers, pop on operator, push result
    return 0
}
```

---

### Pattern 2: Monotonic Stack

---

#### 4. Daily Temperatures — 🟡 Medium
**LeetCode #739** | Solution: [DailyTemperatures.kt](stack/daily_temperatures/DailyTemperatures.kt)

- **Hint 1:** Pattern → Monotonic Decreasing Stack (stores indices)
- **Hint 2:** Stack stores indices waiting for warmer day. When current temp > stack top's temp → pop, result = current - popped index.
- **Hint 3:**
  1. Stack stores indices in decreasing temperature order
  2. For each i: while stack not empty and temps[i] > temps[stack.last()] → pop, result[popped] = i - popped
  3. Push i

**Blank Template:**
```kotlin
fun dailyTemperatures(temperatures: IntArray): IntArray {
    val result = IntArray(temperatures.size)
    val stack = ArrayDeque<Int>()
    // TODO: Monotonic decreasing stack of indices
    return result
}
```

---

#### 5. Next Greater Element I — 🟢 Easy
**LeetCode #496** | Solution: [NextGreaterElementI.kt](stack/next_greater_element_i/NextGreaterElementI.kt)

- **Hint 1:** Pattern → Monotonic Stack + HashMap
- **Hint 2:** Process nums2 once with stack → build map (element → next greater). Lookup nums1 values in map.
- **Hint 3:**
  1. For each num in nums2: while stack not empty and num > stack.last() → nextGreater[pop] = num
  2. Push num
  3. Return nums1.map { nextGreater.getOrDefault(it, -1) }

**Blank Template:**
```kotlin
fun nextGreaterElement(nums1: IntArray, nums2: IntArray): IntArray {
    val nextGreater = hashMapOf<Int, Int>()
    val stack = ArrayDeque<Int>()
    // TODO: Build map from nums2, lookup nums1
    return intArrayOf()
}
```

---

#### 6. Next Greater Element II — 🟡 Medium
**LeetCode #503** | Solution: [NextGreaterElementII.kt](stack/next_greater_element_ii/NextGreaterElementII.kt)

- **Hint 1:** Pattern → Monotonic Stack (Circular Array)
- **Hint 2:** Iterate 2×N using modulo. Only push indices during first pass (i < N).
- **Hint 3:**
  1. For i in 0 until 2*n: idx = i % n
  2. While stack not empty and nums[idx] > nums[stack.last()] → result[pop] = nums[idx]
  3. If i < n → push idx

**Blank Template:**
```kotlin
fun nextGreaterElements(nums: IntArray): IntArray {
    val n = nums.size
    val result = IntArray(n) { -1 }
    val stack = ArrayDeque<Int>()
    // TODO: Iterate 2*N with modulo, push only first pass
    return result
}
```

---

#### 7. Online Stock Span — 🟡 Medium
**LeetCode #901** | Solution: [OnlineStockSpan.kt](stack/online_stock_span/OnlineStockSpan.kt)

- **Hint 1:** Pattern → Monotonic Stack (stores price, span pairs)
- **Hint 2:** When new price ≥ stack top's price → pop and accumulate span. Each popped element's span is absorbed.
- **Hint 3:**
  1. Stack stores Pair(price, span)
  2. span = 1. While stack not empty and top.price <= price → span += pop.span
  3. Push (price, span), return span

**Blank Template:**
```kotlin
class StockSpanner {
    private val stack = ArrayDeque<Pair<Int, Int>>()
    fun next(price: Int): Int {
        var span = 1
        // TODO: Pop smaller prices, accumulate span
        return span
    }
}
```

---

### Pattern 3: Stack — Parentheses

---

#### 8. Minimum Remove to Make Valid Parentheses — 🟡 Medium
**LeetCode #1249** | Solution: [MinimumParentheseToRemove.kt](stack/minimum_parentheses_to_remove/MinimumParentheseToRemove.kt)

- **Hint 1:** Pattern → Stack with Index Marking
- **Hint 2:** Push '(' indices. On ')': if stack not empty → pop (matched), else mark for removal. Remaining '(' in stack → mark for removal.
- **Hint 3:**
  1. Stack stores indices of '('
  2. On ')': if stack not empty → pop, else add index to toRemove set
  3. After pass: addAll remaining stack to toRemove
  4. Build result excluding toRemove indices

**Blank Template:**
```kotlin
fun minRemoveToMakeValid(s: String): String {
    val stack = ArrayDeque<Int>()
    val toRemove = mutableSetOf<Int>()
    // TODO: Track '(' indices, mark unmatched, filter result
    return ""
}
```

---

#### 9. Longest Valid Parentheses — 🔴 Hard
**LeetCode #32** | Solution: [LongestValidParentheses.kt](stack/longest_valid_parentheses/LongestValidParentheses.kt)

- **Hint 1:** Pattern → Stack with Index (boundary tracking)
- **Hint 2:** Push -1 initially as base. On '(' push index. On ')' pop. If stack empty → push current index (new boundary). Else length = i - stack.top().
- **Hint 3:**
  1. stack.push(-1) as base
  2. On '(': push(i)
  3. On ')': pop(). If stack empty → push(i) (boundary). Else maxLength = max(maxLength, i - stack.peek())

**Blank Template:**
```kotlin
fun longestValidParentheses(s: String): Int {
    val stack = java.util.Stack<Int>()
    stack.push(-1)
    var maxLength = 0
    // TODO: Push '(' indices, pop on ')', track boundary
    return maxLength
}
```

---

### Pattern 4: Stack — Advanced

---

#### 10. Largest Rectangle in Histogram — 🔴 Hard
**LeetCode #84** | Solution: [LargestRectangleInHistogram.kt](stack/largest_rectangle_in_histogram/LargestRectangleInHistogram.kt)

- **Hint 1:** Pattern → Monotonic Increasing Stack (indices)
- **Hint 2:** When a smaller height is found, pop and calculate area. Width = current_index - stack.top - 1 (or current_index if empty). Add sentinel 0 at end to flush.
- **Hint 3:**
  1. Append 0 to heights (sentinel to flush stack)
  2. For each i: while stack not empty and heights[i] < heights[stack.last()] → pop, height = heights[popped], width = if empty then i else i - stack.last() - 1
  3. maxArea = max(maxArea, height * width)

**Blank Template:**
```kotlin
fun largestRectangleArea(heights: IntArray): Int {
    val stack = ArrayDeque<Int>()
    var maxArea = 0
    val extended = heights + intArrayOf(0)
    // TODO: Monotonic increasing stack, pop on smaller, calculate area
    return maxArea
}
```

---

#### 11. Car Fleet — 🟡 Medium
**LeetCode #853** | Solution: [CarFleet.kt](stack/car_fleet/CarFleet.kt)

- **Hint 1:** Pattern → Monotonic Stack (Sort by position DESC)
- **Hint 2:** Time to target = (target - position) / speed. If current time ≤ top → merges. If > top → new fleet.
- **Hint 3:**
  1. Sort cars by position descending
  2. For each car: time = (target - pos) / speed
  3. If stack empty or time > stack.last() → push time (new fleet)
  4. Else: merges (don't push)
  5. Return stack.size

**Blank Template:**
```kotlin
fun carFleet(target: Int, position: IntArray, speed: IntArray): Int {
    val cars = position.indices.map { Pair(position[it], speed[it]) }.sortedByDescending { it.first }
    val stack = ArrayDeque<Double>()
    // TODO: Sort by position DESC, push times, merge if <= top
    return stack.size
}
```

---

### Pattern 5: Simulation — Basic

---

#### 12. Add Binary — 🟢 Easy
**LeetCode #67** | Solution: [AddBinary.kt](simulation/add_binary/AddBinary.kt)

- **Hint 1:** Pattern → Simulation (Digit-by-Digit with Carry)
- **Hint 2:** Traverse right to left. sum = bit_a + bit_b + carry. result bit = sum % 2, carry = sum / 2. Reverse at end.
- **Hint 3:**
  1. i = a.length-1, j = b.length-1, carry = 0
  2. While i >= 0 or j >= 0 or carry == 1: sum = carry + a[i] + b[j]
  3. Append sum % 2, carry = sum / 2
  4. Reverse result

**Blank Template:**
```kotlin
fun addBinary(a: String, b: String): String {
    val result = StringBuilder()
    var i = a.length - 1; var j = b.length - 1; var carry = 0
    // TODO: Right to left, sum % 2 for bit, sum / 2 for carry
    return result.reverse().toString()
}
```

---

#### 13. Add Strings — 🟢 Easy
**LeetCode #415** | Solution: [AddString.kt](simulation/add_string/AddString.kt)

- **Hint 1:** Pattern → Simulation (Digit-by-Digit with Carry)
- **Hint 2:** Same as Add Binary but base 10. sum = d1 + d2 + carry. result digit = sum % 10, carry = sum / 10.
- **Hint 3:**
  1. i = num1.length-1, j = num2.length-1, carry = 0
  2. d1 = if i >= 0 then num1[i--] - '0' else 0
  3. sum = d1 + d2 + carry, append sum % 10, carry = sum / 10
  4. Reverse result

**Blank Template:**
```kotlin
fun addStrings(num1: String, num2: String): String {
    val result = StringBuilder()
    var i = num1.length - 1; var j = num2.length - 1; var carry = 0
    // TODO: Right to left, sum % 10 for digit, sum / 10 for carry
    return result.reverse().toString()
}
```

---

#### 14. FizzBuzz — 🟢 Easy
**LeetCode #412** | Solution: [FizzBuzz.kt](simulation/fizz_buzz/FizzBuzz.kt)

- **Hint 1:** Pattern → Simulation (Modulo checks)
- **Hint 2:** Check divisible by 3 AND 5 first (FizzBuzz), then 3 (Fizz), then 5 (Buzz), else the number.
- **Hint 3:**
  1. For i in 1..n
  2. If i % 3 == 0 && i % 5 == 0 → "FizzBuzz"
  3. Else if i % 3 == 0 → "Fizz"
  4. Else if i % 5 == 0 → "Buzz"
  5. Else → "$i"

**Blank Template:**
```kotlin
fun fizzBuzz(n: Int): List<String> {
    val result = mutableListOf<String>()
    // TODO: Check 3&5 first, then 3, then 5, else number
    return result
}
```

---

#### 15. Add Digits — 🟢 Easy
**LeetCode #258** | Solution: [AddDigits.kt](simulation/add_digits/AddDigits.kt)

- **Hint 1:** Pattern → Simulation (Digital Root)
- **Hint 2:** Repeatedly sum digits until single digit. O(1) trick: if num == 0 → 0, if num % 9 == 0 → 9, else num % 9.
- **Hint 3:**
  1. While n > 9: sum all digits, n = sum
  2. O(1): return when { num == 0 → 0; num % 9 == 0 → 9; else → num % 9 }

**Blank Template:**
```kotlin
fun addDigits(num: Int): Int {
    var n = num
    // TODO: While n > 9, sum digits. Or use digital root formula.
    return n
}
```

---

### Pattern 6: Simulation — Matrix Traversalal

---

#### 16. Spiral Matrix — 🟡 Medium
**LeetCode #54** | Solution: [SpiralMatrix.kt](array/matrix/spiral_matrix/SpiralMatrix.kt)

- **Hint 1:** Pattern → Simulation (Four Boundaries)
- **Hint 2:** Use top, bottom, left, right boundaries. Traverse: right → down → left → up. Shrink boundaries after each direction.
- **Hint 3:**
  1. While top <= bottom and left <= right
  2. Move right (top row), top++
  3. Move down (right col), right--
  4. If top <= bottom: move left (bottom row), bottom--
  5. If left <= right: move up (left col), left++

**Blank Template:**
```kotlin
fun spiralOrder(matrix: Array<IntArray>): List<Int> {
    val result = mutableListOf<Int>()
    var top = 0; var bottom = matrix.size - 1
    var left = 0; var right = matrix[0].size - 1
    // TODO: Four-directional traversal with shrinking boundaries
    return result
}
```

---

#### 17. Spiral Matrix II — 🟡 Medium
**LeetCode #59** | Solution: [SpiralMatrixII.kt](simulation/spiral_matrix_ii/SpiralMatrixII.kt)

- **Hint 1:** Pattern → Simulation (Four Boundaries, Fill)
- **Hint 2:** Same as Spiral Matrix but fill instead of read. Counter from 1 to n².
- **Hint 3:**
  1. Create n×n result array, count = 1
  2. While top <= bottom and left <= right
  3. Move right: result[top][i] = count++, top++
  4. Move down: result[i][right] = count++, right--
  5. If top <= bottom: move left, bottom--
  6. If left <= right: move up, left++

**Blank Template:**
```kotlin
fun generateMatrix(n: Int): Array<IntArray> {
    val result = Array(n) { IntArray(n) }
    var top = 0; var bottom = n - 1; var left = 0; var right = n - 1
    var count = 1
    // TODO: Four-directional fill with count++
    return result
}
```

---

#### 18. Text Justification — 🔴 Hard
**LeetCode #68** | Solution: [TextJustification.kt](simulation/text_justification/TextJustification.kt)

- **Hint 1:** Pattern → Simulation (Greedy Line Packing)
- **Hint 2:** Greedily pack words per line. Last line or single word → left justify. Otherwise distribute spaces evenly (extra spaces go left first).
- **Hint 3:**
  1. Find words that fit: lineLength + 1 + words[j].length <= maxWidth
  2. numSpaces = maxWidth - (lineLength - (numWords - 1))
  3. Last line or 1 word: left justify, pad with spaces
  4. Else: baseSpaces = numSpaces / slots, extra = numSpaces % slots. First `extra` slots get +1 space.

**Blank Template:**
```kotlin
fun fullJustify(words: Array<String>, maxWidth: Int): List<String> {
    val result = mutableListOf<String>()
    var i = 0
    // TODO: Pack words, distribute spaces, left-justify last line
    return result
}
```

---

### Pattern 7: Linked List — Basic

---

#### 19. Reverse Linked List — 🟢 Easy
**LeetCode #206** | Solution: [ReverseLinkedList.kt](linked_list/single/reverse_linked_list/ReverseLinkedList.kt)

- **Hint 1:** Pattern → Iterative Three-Pointer (prev, current, next)
- **Hint 2:** For each node: save next, point current.next to prev, move prev and current forward.
- **Hint 3:**
  1. prev = null, current = head
  2. While current != null: nextNode = current.next, current.next = prev, prev = current, current = nextNode
  3. Return prev

**Blank Template:**
```kotlin
fun reverseLinkedList(head: ListNode?): ListNode? {
    var current = head
    var prev: ListNode? = null
    // TODO: Save next, reverse link, advance prev and current
    return prev
}
```

---

#### 20. Linked List Cycle — 🟢 Easy
**LeetCode #141** | Solution: [LinkedListCycle.kt](linked_list/single/linked_list_cycle/LinkedListCycle.kt)

- **Hint 1:** Pattern → Floyd's Tortoise & Hare (Fast/Slow Pointer)
- **Hint 2:** Slow moves 1 step, fast moves 2 steps. If they meet → cycle. If fast reaches null → no cycle.
- **Hint 3:**
  1. slow = head, fast = head
  2. While fast != null and fast.next != null: slow = slow.next, fast = fast.next.next
  3. If slow == fast → return true
  4. Return false

**Blank Template:**
```kotlin
fun hasCycle(head: ListNode?): Boolean {
    var slow = head; var fast = head
    // TODO: Slow 1 step, fast 2 steps, check if they meet
    return false
}
```

---

#### 21. Merge Two Sorted Lists — 🟢 Easy
**LeetCode #21** | Solution: [MergeTwoSortedLists.kt](linked_list/single/merge_two_sorted_lists/MergeTwoSortedLists.kt)

- **Hint 1:** Pattern → Dummy Head + Two Pointer
- **Hint 2:** Use dummy node. Compare values, attach smaller to tail. Advance the chosen list. Append remaining.
- **Hint 3:**
  1. dummy = ListNode(0), tail = dummy
  2. While a != null and b != null: if a.val <= b.val → tail.next = a, a = a.next, else → tail.next = b, b = b.next. tail = tail.next
  3. tail.next = a ?: b
  4. Return dummy.next

**Blank Template:**
```kotlin
fun mergeTwoLists(l1: ListNode?, l2: ListNode?): ListNode? {
    val dummy = ListNode(0)
    var tail = dummy
    var a = l1; var b = l2
    // TODO: Compare, attach smaller, advance, append remaining
    return dummy.next
}
```

---

#### 22. Remove Nth Node from End — 🟡 Medium
**LeetCode #19** | Solution: [RemoveNthNodeFromLast.kt](linked_list/single/remove_nth_node_from_last/RemoveNthNodeFromLast.kt)

- **Hint 1:** Pattern → Two Pointer (Gap of N)
- **Hint 2:** Use dummy to handle head removal. Move fast N+1 ahead. Then move both until fast reaches end. slow.next = slow.next.next.
- **Hint 3:**
  1. dummy.next = head, fast = dummy, slow = dummy
  2. Move fast N+1 steps ahead
  3. While fast != null: fast = fast.next, slow = slow.next
  4. slow.next = slow.next.next
  5. Return dummy.next

**Blank Template:**
```kotlin
fun removeNthFromEnd(head: ListNode?, n: Int): ListNode? {
    val dummy = ListNode(0).apply { next = head }
    var fast: ListNode? = dummy; var slow: ListNode? = dummy
    // TODO: Move fast N+1 ahead, then both until fast is null, skip slow.next
    return dummy.next
}
```

---

#### 23. Middle of Linked List — 🟢 Easy
**LeetCode #876** | Solution: [MiddleOfLinkedList.kt](linked_list/single/middle_of_linked_list/MiddleOfLinkedList.kt)

- **Hint 1:** Pattern → Fast/Slow Pointer
- **Hint 2:** Fast moves 2 steps, slow moves 1. When fast reaches end, slow is at middle.
- **Hint 3:**
  1. slow = head, fast = head
  2. While fast != null and fast.next != null: slow = slow.next, fast = fast.next.next
  3. Return slow

**Blank Template:**
```kotlin
fun middleNode(head: ListNode?): ListNode? {
    var slow = head; var fast = head
    // TODO: Fast 2x, slow 1x, return slow when fast reaches end
    return slow
}
```

---

#### 24. Palindrome Linked List — 🟡 Medium
**LeetCode #234** | Solution: [PalindromeLinkedList.kt](linked_list/single/palindrome_linked_list/PalindromeLinkedList.kt)

- **Hint 1:** Pattern → Fast/Slow + Reverse Second Half
- **Hint 2:** Find middle, reverse second half, compare first half with reversed second half.
- **Hint 3:**
  1. Fast/slow to find middle
  2. Reverse from middle to end (prev, curr pattern)
  3. Compare first (head) with second (prev) — if mismatch → false

**Blank Template:**
```kotlin
fun isPalindrome(head: ListNode?): Boolean {
    // Step 1: Find middle (fast/slow)
    // Step 2: Reverse second half
    // Step 3: Compare first half with reversed second half
    return true
}
```

---

### Pattern 8: HashSet Lookup (Review)

---

#### 25. Valid Sudoku — 🟡 Medium
**LeetCode #36** | Solution: [ValidSudoku.kt](array/hashset_lookup/valid_sudoku/ValidSudoku.kt)

- **Hint 1:** Pattern → HashSet Lookup (3 sets: rows, cols, boxes)
- **Hint 2:** Box index = (row / 3) * 3 + (col / 3). Check if digit already in row, col, or box set.
- **Hint 3:**
  1. 9 sets for rows, 9 for cols, 9 for boxes
  2. For each cell: if '.' → skip. boxIndex = (i/3)*3 + (j/3)
  3. If c in rows[i] or cols[j] or boxes[boxIndex] → false
  4. Add to all three sets

**Blank Template:**
```kotlin
fun isValidSudoku(board: Array<CharArray>): Boolean {
    val rows = Array(9) { mutableSetOf<Char>() }
    val cols = Array(9) { mutableSetOf<Char>() }
    val boxes = Array(9) { mutableSetOf<Char>() }
    // TODO: Check each cell against row, col, box sets
    return true
}
```

---

#### 26. Contains Duplicates (review) — 🟢 Easy
**LeetCode #217** | Solution: [ContainsDuplicates.kt](array/hashset_lookup/contains_duplicates/ContainsDuplicates.kt)

- **Hint 1:** Pattern → HashSet Lookup
- **Hint 2:** Add each element to HashSet. If already exists → duplicate found.
- **Hint 3:**
  1. Create empty HashSet
  2. For each num: if in set → return true, else add
  3. Return false

**Blank Template:**
```kotlin
fun containsDuplicate(nums: IntArray): Boolean {
    val set = mutableSetOf<Int>()
    // TODO: Check existence as you add
    return false
}
```

---

### Pattern 9: Two Pointer (Review + New)

---

#### 27. Two Sum II — Sorted Array (review) — 🟡 Medium
**LeetCode #167** | Solution: [TwoSumLevelTwoWithSortedArray.kt](array/two_pointer/two_sum_level_two_with_sorted_array/TwoSumLevelTwoWithSortedArray.kt)

- **Hint 1:** Pattern → Two Pointer (sorted array)
- **Hint 2:** Converge from both ends. Sum too small → move left. Sum too big → move right.
- **Hint 3:**
  1. left = 0, right = n-1
  2. If sum < target → left++
  3. If sum > target → right--
  4. If equal → return [left+1, right+1] (1-indexed)

**Blank Template:**
```kotlin
fun twoSumSorted(numbers: IntArray, target: Int): IntArray {
    var left = 0; var right = numbers.size - 1
    // TODO: Converge from both ends, compare sum with target
    return intArrayOf()
}
```

---

#### 28. Valid Palindrome — 🟢 Easy
**LeetCode #125** | Solution: [ValidPalindrom.kt](array/two_pointer/valid_palindrom/ValidPalindrom.kt)

- **Hint 1:** Pattern → Two Pointer (skip non-alphanumeric)
- **Hint 2:** Compare from both ends, skipping non-alphanumeric characters. Lowercase both before comparing.
- **Hint 3:**
  1. left = 0, right = s.length - 1
  2. While left < right: skip non-alphanumeric on both sides
  3. If s[left].lowercaseChar() != s[right].lowercaseChar() → false
  4. left++, right--

**Blank Template:**
```kotlin
fun isPalindrome(s: String): Boolean {
    var left = 0; var right = s.length - 1
    // TODO: Skip non-alphanumeric, compare lowercase
    return true
}
```

---

#### 29. Is Subsequence — 🟢 Easy
**LeetCode #392** | Solution: [IsSubSequence.kt](array/two_pointer/is_sub_sequence/IsSubSequence.kt)

- **Hint 1:** Pattern → Two Pointer (single pass through t)
- **Hint 2:** Iterate through t. If s[sIndex] == t[tIndex] → sIndex++. If sIndex == s.length → true.
- **Hint 3:**
  1. sIndex = 0
  2. For each char in t: if sIndex < s.length and s[sIndex] == char → sIndex++
  3. Return sIndex == s.length

**Blank Template:**
```kotlin
fun isSubsequence(s: String, t: String): Boolean {
    var sIndex = 0
    // TODO: Iterate t, advance sIndex on match
    return sIndex == s.length
}
```

---

### Pattern 10: Greedy — Intervals (Review + New)

---

#### 30. Merge Intervals — 🟡 Medium
**LeetCode #56** | Solution: [MergeIntervals.kt](array/greedy/merge_intervals/MergeIntervals.kt)

- **Hint 1:** Pattern → Greedy (Sort by Start + Single Pass)
- **Hint 2:** Sort by start. If current start ≤ last merged end → merge (extend end). Else → add new interval.
- **Hint 3:**
  1. Sort by start time
  2. result = [intervals[0]]
  3. For each interval: if start ≤ result.last().end → result.last().end = max(end, current.end)
  4. Else → add to result

**Blank Template:**
```kotlin
fun merge(intervals: Array<IntArray>): Array<IntArray> {
    intervals.sortBy { it[0] }
    val result = mutableListOf(intervals[0])
    // TODO: Merge if overlap, else add new
    return result.toTypedArray()
}
```

---

#### 31. Meeting Rooms — 🟡 Medium
**LeetCode #252** | Solution: [MeetingRooms.kt](array/greedy/meeting_rooms/MeetingRooms.kt)

- **Hint 1:** Pattern → Greedy (Sort by Start)
- **Hint 2:** Sort by start time. If any meeting starts before the previous one ends → conflict.
- **Hint 3:**
  1. Sort intervals by start time
  2. For i in 1 until n: if intervals[i].start < intervals[i-1].end → false
  3. Return true

**Blank Template:**
```kotlin
fun canAttendMeetings(intervals: Array<IntArray>): Boolean {
    intervals.sortBy { it[0] }
    // TODO: Check if any meeting starts before previous ends
    return true
}
```

---

#### 32. Non-Overlapping Intervals — 🟡 Medium
**LeetCode #435** | Solution: [NonOverlappingIntervals.kt](array/greedy/non_overlapping_intervals/NonOverlappingIntervals.kt)

- **Hint 1:** Pattern → Greedy (Sort by End Time)
- **Hint 2:** Sort by end. If current start < lastEnd → overlap → remove (count++). Else → update lastEnd.
- **Hint 3:**
  1. Sort by end time
  2. lastEnd = intervals[0].end, removals = 0
  3. For each interval from 1: if start < lastEnd → removals++. Else → lastEnd = end
  4. Return removals

**Blank Template:**
```kotlin
fun eraseOverlapIntervals(intervals: Array<IntArray>): Int {
    intervals.sortBy { it[1] }
    var removals = 0; var lastEnd = intervals[0][1]
    // TODO: If start < lastEnd → remove, else update lastEnd
    return removals
}
```

---

#### 33. Gas Station (review) — 🟡 Medium
**LeetCode #134** | Solution: [GasStation.kt](array/greedy/gas_station/GasStation.kt)

- **Hint 1:** Pattern → Greedy (One Pass)
- **Hint 2:** If totalGas >= totalCost, solution exists. If currentTank < 0 at station i, start fresh at i+1.
- **Hint 3:**
  1. Track totalTank (overall surplus) and currentTank (running from start)
  2. If currentTank < 0 → reset startStation = i+1, currentTank = 0
  3. If totalTank >= 0 → return startStation, else -1

**Blank Template:**
```kotlin
fun canCompleteCircuit(gas: IntArray, cost: IntArray): Int {
    var totalTank = 0; var currentTank = 0; var startStation = 0
    // TODO: Track total, reset start when currentTank < 0
    return -1
}
```

---

### Pattern 11: Matrix (Review + New)

---

#### 34. Rotate Image — 🟡 Medium
**LeetCode #48** | Solution: [RotateImage.kt](array/matrix/rotate_image/RotateImage.kt)

- **Hint 1:** Pattern → Matrix (Transpose + Reverse Each Row)
- **Hint 2:** 90° clockwise = transpose (swap [i][j] with [j][i]) then reverse each row.
- **Hint 3:**
  1. Transpose: for i in 0 until n, for j in i+1 until n → swap matrix[i][j] and matrix[j][i]
  2. Reverse each row: two pointer swap within row

**Blank Template:**
```kotlin
fun rotate(matrix: Array<IntArray>) {
    val n = matrix.size
    // Step 1: Transpose (swap [i][j] ↔ [j][i])
    // Step 2: Reverse each row
}
```

---

#### 35. Spiral Matrix (review) — 🟡 Medium
**LeetCode #54** | Solution: [SpiralMatrix.kt](array/matrix/spiral_matrix/SpiralMatrix.kt)

- **Hint 1:** Pattern → Matrix (Four Boundaries)
- **Hint 2:** Same as Simulation Spiral Matrix. Use top, bottom, left, right. Traverse right → down → left → up.
- **Hint 3:**
  1. While top <= bottom and left <= right
  2. Move right (top row), top++
  3. Move down (right col), right--
  4. If top <= bottom: move left, bottom--
  5. If left <= right: move up, left++

**Blank Template:**
```kotlin
fun spiralOrder(matrix: Array<IntArray>): List<Int> {
    val result = mutableListOf<Int>()
    var top = 0; var bottom = matrix.size - 1
    var left = 0; var right = matrix[0].size - 1
    // TODO: Four-directional traversal with shrinking boundaries
    return result
}
```

---

#### 36. Game of Life — 🟡 Medium
**LeetCode #289** | Solution: [GameOfLife.kt](array/matrix/game_of_life/GameOfLife.kt)

- **Hint 1:** Pattern → Matrix (In-Place with Intermediate States)
- **Hint 2:** Use intermediate values: 0→1 = 2 (dead to live), 1→0 = 3 (live to dead). Read original state as 1 or 3.
- **Hint 3:**
  1. First pass: count live neighbors (original live = 1 or 3)
  2. Apply rules: live with <2 or >3 neighbors → 3 (dies). Dead with exactly 3 → 2 (lives)
  3. Second pass: 2 → 1, 3 → 0

**Blank Template:**
```kotlin
fun gameOfLife(board: Array<IntArray>) {
    val m = board.size; val n = board[0].size
    val dirs = arrayOf(-1 to -1, -1 to 0, -1 to 1, 0 to -1, 0 to 1, 1 to -1, 1 to 0, 1 to 1)
    // TODO: Pass 1: mark with 2/3. Pass 2: convert to 0/1
}
```

---

## 📅 Suggested Weekly Practice Plan

| Day | Problems to Practice | Focus |
|-----|---------------------|-------|
| Mon | 2 Easy problems (from "not mastered") | Speed + accuracy |
| Tue | 2 Medium problems (from "not mastered") | Pattern recognition |
| Wed | 1 Hard problem (from "not mastered") | Deep understanding |
| Thu | 2 Easy/Medium (re-attempt failed ones) | Fix weak spots |
| Fri | 2 Medium problems (from "not mastered") | Timed (15 min each) |
| Sat | 1 Hard + 1 Easy review | Mixed difficulty |
| Sun | Rest or re-solve 3 "almost mastered" | Push to mastered |

---

*Back to [Week 1-2 Tracker](Week1-2_Practice_Tracker.md) | [Revision Notes](Revision_Notes.md) | [Daily DSA Schedule](../interview_schedule/Daily_DSA_Schedule.md) | [Pattern Index](PatternWiseProblems.md)*