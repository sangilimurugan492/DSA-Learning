# 🎯 DSA Guided Practice Tracker — Week 5 & 6

> **Purpose:** Practice problems you've already solved — with progressive hints so you never get fully stuck.
> **Goal:** Wean off looking at solutions. Go from `📖 Read` → `💡 Hints` → `✅ From scratch` → `🏆 Mastered`
> **Problems:** 40 (Week 5: 20 Tree + Heap + Trie + Bit Manipulation | Week 6: 20 Bit Manipulation + Tree Traversal + String Parsing + Set Ops + Voting & Greedy)
> **Scope:** Week 5 & 6 only. Trackers for later weeks will be created as you progress.

---

## 📖 How to Use This File

### The 4-Day Re-solve Cycle (per problem)

| Day | What to Do | Mark |
|-----|-----------|------|
| **Day 1** | Read the solution, understand it fully. Close the file. | 📖 |
| **Day 2** | Try to solve using only **Hint 1** (pattern name). If stuck, reveal Hint 2. | 💡 |
| **Day 3** | Try to solve using only **Hint 2** (key insight). If stuck, reveal Hint 3. | 💡 |
| **Day 4** | Try to solve **from scratch** — no hints. | ✅ or 🔄 |

- **3 successful from-scratch solves** = 🏆 **Mastered**
- If you fail from scratch, go back to Day 2 and repeat the cycle

### Daily Practice Routine (30 min)
1. Pick **2 problems** from your "not yet mastered" list
2. Follow the cycle for each
3. Update the tracker table below

---

## 📅 Date-Based Preparation Schedule

> Each day maps to the DSA practice slot in your daily checklist. Follow this schedule to stay on track.

### Week 5 — Sep 17 (Thu) to Sep 23 (Wed)

| Date | Day | Problems to Practice | Pattern Focus | Time |
|------|-----|---------------------|---------------|------|
| **Sep 17 (Thu)** | Day 29 | #1 Maximum Depth of Binary Tree, #2 Same Tree | Tree (Binary Tree) | 30 min |
| **Sep 18 (Fri)** | Day 30 | #3 Search in BST, #4 Validate BST | Tree (BST) | 30 min |
| **Sep 19 (Sat)** | Day 31 | #5 Kth Largest Element, #6 Top K Frequent Elements, #7 Merge K Sorted Lists, #8 Find Median from Data Stream | Heap | 1.5 hr |
| **Sep 20 (Sun)** | Day 32 | #9 Balanced Binary Tree, #10 Diameter of Binary Tree, #11 Insert into BST, #12 Delete Node in BST | Tree (Binary Tree + BST) | 1.5 hr |
| **Sep 21 (Mon)** | Day 33 | #13 Implement Trie, #14 Word Search II | Trie | 30 min |
| **Sep 22 (Tue)** | Day 34 | #15 Single Number, #16 Single Number II | Bit Manipulation | 30 min |
| **Sep 23 (Wed)** | Day 35 | #17 Number of 1 Bits, #18 Missing Number, #19 Subtree of Another Tree, #20 Kth Smallest in BST | Bit Manipulation + Tree Review | 1 hr |

### Week 6 — Sep 24 (Thu) to Sep 30 (Wed)

| Date | Day | Problems to Practice | Pattern Focus | Time |
|------|-----|---------------------|---------------|------|
| **Sep 24 (Thu)** | Day 36 | #21 Sum of Two Integers, #22 Power of Two | Bit Manipulation | 30 min |
| **Sep 25 (Fri)** | Day 37 | #23 Counting Bits, #24 Lowest Common Ancestor | Bit Manipulation + Tree | 30 min |
| **Sep 26 (Sat)** | Day 38 | #25 Tree Traversals, #26 Level Order Traversal, #27 Right Side View, #28 Invert Binary Tree | Tree Traversal | 1.5 hr |
| **Sep 27 (Sun)** | Day 39 | #29 Integer to Roman, #30 Longest Palindromic String, #31 ZigZag Conversion, #32 Group Anagrams | String Parsing + Set Operations | 1.5 hr |
| **Sep 28 (Mon)** | Day 40 | #33 Find Difference Between Two Arrays, #34 Count the Number of Pairs | Set Operations + Frequency Count | 30 min |
| **Sep 29 (Tue)** | Day 41 | #35 Majority Element, #36 Majority Element II | Voting & Floyd's | 30 min |
| **Sep 30 (Wed)** | Day 42 | #37 Find Duplicate Number, #38 Missing Number in Array, #39 Lemonade Change, #40 Jump Game II | Voting & Floyd's + Greedy | 1 hr |

---

## 📊 Master Tracker

| # | Problem | Pattern | Difficulty | Attempt 1 | Attempt 2 | Attempt 3 | Status |
|---|---------|---------|-----------|-----------|-----------|-----------|--------|
| 1 | Maximum Depth of Binary Tree | Tree (DFS) | 🟢 |           | | | ⬜      |
| 2 | Same Tree | Tree (DFS) | 🟢 |           | | | ⬜      |
| 3 | Search in BST | Tree (BST) | 🟢 |           | | | ⬜      |
| 4 | Validate BST | Tree (BST) | 🟡 |           | | | ⬜      |
| 5 | Kth Largest Element | Heap (Min-Heap) | 🟡 |           | | | ⬜      |
| 6 | Top K Frequent Elements | Heap + HashMap | 🟡 |           | | | ⬜      |
| 7 | Merge K Sorted Lists | Heap (Min-Heap) | 🔴 |           | | | ⬜      |
| 8 | Find Median from Data Stream | Heap (Two Heaps) | 🔴 |           | | | ⬜      |
| 9 | Balanced Binary Tree | Tree (DFS) | 🟢 |           | | | ⬜      |
| 10 | Diameter of Binary Tree | Tree (DFS) | 🟡 |           | | | ⬜      |
| 11 | Insert into BST | Tree (BST) | 🟡 |           | | | ⬜      |
| 12 | Delete Node in BST | Tree (BST) | 🟡 |           | | | ⬜      |
| 13 | Implement Trie (Prefix Tree) | Trie | 🟡 |           | | | ⬜      |
| 14 | Word Search II | Trie + Backtracking | 🔴 |           | | | ⬜      |
| 15 | Single Number | Bit Manipulation (XOR) | 🟢 |           | | | ⬜      |
| 16 | Single Number II | Bit Manipulation | 🟡 |           | | | ⬜      |
| 17 | Number of 1 Bits | Bit Manipulation | 🟢 |           | | | ⬜      |
| 18 | Missing Number | Bit Manipulation (XOR) | 🟢 |           | | | ⬜      |
| 19 | Subtree of Another Tree | Tree (DFS) | 🟢 |           | | | ⬜      |
| 20 | Kth Smallest Element in BST | Tree (BST In-order) | 🟡 |           | | | ⬜      |
| 21 | Sum of Two Integers | Bit Manipulation | 🟡 |           | | | ⬜      |
| 22 | Power of Two | Bit Manipulation | 🟢 |           | | | ⬜      |
| 23 | Counting Bits | Bit Manipulation (DP) | 🟡 |           | | | ⬜      |
| 24 | Lowest Common Ancestor | Tree (DFS) | 🟡 |           | | | ⬜      |
| 25 | Tree Traversals (In/Pre/Post) | Tree (DFS) | 🟢 |           | | | ⬜      |
| 26 | Binary Tree Level Order Traversal | Tree (BFS) | 🟡 |           | | | ⬜      |
| 27 | Binary Tree Right Side View | Tree (BFS) | 🟡 |           | | | ⬜      |
| 28 | Invert Binary Tree | Tree (DFS) | 🟢 |           | | | ⬜      |
| 29 | Integer to Roman | String Parsing (Greedy) | 🟡 |           | | | ⬜      |
| 30 | Longest Palindromic String | String Parsing (Expand) | 🟡 |           | | | ⬜      |
| 31 | ZigZag Conversion | String Parsing (Simulation) | 🟡 |           | | | ⬜      |
| 32 | Group Anagrams | Set Operations (Sort Key) | 🟡 |           | | | ⬜      |
| 33 | Find Difference Between Two Arrays | Set Operations | 🟢 |           | | | ⬜      |
| 34 | Count the Number of Pairs | Frequency Count | 🟡 |           | | | ⬜      |
| 35 | Majority Element | Voting (Boyer-Moore) | 🟢 |           | | | ⬜      |
| 36 | Majority Element II | Voting (Boyer-Moore) | 🟡 |           | | | ⬜      |
| 37 | Find Duplicate Number | Floyd's (Cycle Detection) | 🟡 |           | | | ⬜      |
| 38 | Missing Number in Array | Floyd's / Math | 🟢 |           | | | ⬜      |
| 39 | Lemonade Change | Greedy | 🟢 |           | | | ⬜      |
| 40 | Jump Game II | Greedy (BFS-like) | 🟡 |           | | | ⬜      |

**Legend:** ⬜ Not started | 📖 Read solution | 💡 Used hints | ✅ From scratch | 🏆 Mastered

---

## 🧩 Problems with Progressive Hints

### Pattern 1: Tree — Binary Tree (DFS)

---

#### 1. Maximum Depth of Binary Tree — 🟢 Easy
**LeetCode #104** | Solution: [MaximumDepthOfBinaryTree.kt](tree/binary_tree/MaximumDepthOfBinaryTree.kt)

- **Hint 1:** Pattern → DFS (Recursive)
- **Hint 2:** Depth = 1 + max(depth(left), depth(right)). Base case: null → 0.
- **Hint 3:**
  1. If root == null → return 0
  2. leftDepth = maxDepth(root.left)
  3. rightDepth = maxDepth(root.right)
  4. Return 1 + max(leftDepth, rightDepth)

**Blank Template:**
```kotlin
fun maxDepth(root: TreeNode?): Int {
    // TODO: Base case null → 0, else 1 + max(left, right)
    return 0
}
```

---

#### 2. Same Tree — 🟢 Easy
**LeetCode #100** | Solution: [SameTree.kt](tree/binary_tree/SameTree.kt)

- **Hint 1:** Pattern → DFS (Recursive, compare nodes)
- **Hint 2:** Both null → true. One null → false. Values differ → false. Recurse left and right.
- **Hint 3:**
  1. If p == null && q == null → true
  2. If p == null || q == null → false
  3. If p.`val` != q.`val` → false
  4. Return isSameTree(p.left, q.left) && isSameTree(p.right, q.right)

**Blank Template:**
```kotlin
fun isSameTree(p: TreeNode?, q: TreeNode?): Boolean {
    // TODO: Both null → true, one null → false, compare values, recurse
    return false
}
```

---

#### 9. Balanced Binary Tree — 🟢 Easy
**LeetCode #110** | Solution: [BalancedBinaryTree.kt](tree/binary_tree/BalancedBinaryTree.kt)

- **Hint 1:** Pattern → DFS (check height difference)
- **Hint 2:** A tree is balanced if |leftHeight - rightHeight| <= 1 AND both subtrees are balanced.
- **Hint 3:**
  1. Helper function height(node): returns -1 if unbalanced, else height
  2. left = height(node.left), right = height(node.right)
  3. If left == -1 or right == -1 or |left - right| > 1 → return -1
  4. Return 1 + max(left, right)

**Blank Template:**
```kotlin
fun isBalanced(root: TreeNode?): Boolean {
    // TODO: Use height helper that returns -1 if unbalanced
    return false
}
```

---

#### 10. Diameter of Binary Tree — 🟡 Medium
**LeetCode #543** | Solution: [DiameterOfBinaryTree.kt](tree/binary_tree/DiameterOfBinaryTree.kt)

- **Hint 1:** Pattern → DFS (track max diameter during height calculation)
- **Hint 2:** Diameter through a node = leftHeight + rightHeight. Track the global max. Return height to parent.
- **Hint 3:**
  1. Global variable `diameter = 0`
  2. height(node): left = height(node.left), right = height(node.right)
  3. diameter = max(diameter, left + right)
  4. Return 1 + max(left, right)

**Blank Template:**
```kotlin
fun diameterOfBinaryTree(root: TreeNode?): Int {
    var diameter = 0
    // TODO: DFS, update diameter = max(diameter, left + right)
    return diameter
}
```

---

#### 19. Subtree of Another Tree — 🟢 Easy
**LeetCode #572** | Solution: [SubtreeOfAnotherTree.kt](tree/binary_tree/SubtreeOfAnotherTree.kt)

- **Hint 1:** Pattern → DFS (check each node as potential root of subtree)
- **Hint 2:** For each node in main tree, check if subtree starting there matches target. Use isSameTree helper.
- **Hint 3:**
  1. If root == null → return false
  2. If isSameTree(root, subRoot) → true
  3. Return isSubtree(root.left, subRoot) || isSubtree(root.right, subRoot)

**Blank Template:**
```kotlin
fun isSubtree(root: TreeNode?, subRoot: TreeNode?): Boolean {
    // TODO: Check sameTree at each node, recurse left and right
    return false
}
```

---

#### 24. Lowest Common Ancestor — 🟡 Medium
**LeetCode #236** | Solution: [LowestCommonAncestor.kt](tree/binary_tree/LowestCommonAncestor.kt)

- **Hint 1:** Pattern → DFS (recursive return)
- **Hint 2:** If current node is p or q → return it. If p and q are found in different subtrees → current is LCA.
- **Hint 3:**
  1. If root == null or root == p or root == q → return root
  2. left = LCA(root.left, p, q), right = LCA(root.right, p, q)
  3. If left != null and right != null → return root (LCA found)
  4. Return left ?: right

**Blank Template:**
```kotlin
fun lowestCommonAncestor(root: TreeNode?, p: TreeNode?, q: TreeNode?): TreeNode? {
    // TODO: Return node if it's p or q, recurse both sides, if both non-null → root
    return null
}
```

---

#### 28. Invert Binary Tree — 🟢 Easy
**LeetCode #226** | Solution: [InvertBinaryTree.kt](tree/binary_tree/invert_binary_tree/InvertBinaryTree.kt)

- **Hint 1:** Pattern → DFS (swap children)
- **Hint 2:** Swap left and right children at each node. Recurse into both subtrees.
- **Hint 3:**
  1. If root == null → return null
  2. Swap root.left and root.right
  3. invertTree(root.left), invertTree(root.right)
  4. Return root

**Blank Template:**
```kotlin
fun invertTree(root: TreeNode?): TreeNode? {
    // TODO: Base case null, swap children, recurse, return root
    return root
}
```

---

### Pattern 2: Tree — BST

---

#### 3. Search in BST — 🟢 Easy
**LeetCode #700** | Solution: [SearchInBST.kt](tree/bst/SearchInBST.kt)

- **Hint 1:** Pattern → BST Search (compare and go left/right)
- **Hint 2:** If val < node.val → go left. If val > node.val → go right. If equal → found.
- **Hint 3:**
  1. While node != null: if val == node.`val` → return node
  2. If val < node.`val` → node = node.left, else → node = node.right
  3. Return null

**Blank Template:**
```kotlin
fun searchBST(root: TreeNode?, `val`: Int): TreeNode? {
    // TODO: Compare val, go left or right, return node if found
    return null
}
```

---

#### 4. Validate BST — 🟡 Medium
**LeetCode #98** | Solution: [ValidateBST.kt](tree/bst/ValidateBST.kt)

- **Hint 1:** Pattern → BST Validation (min/max bounds)
- **Hint 2:** Pass down (min, max) bounds. Each node must be within its allowed range.
- **Hint 3:**
  1. Helper(node, min, max): if node == null → true
  2. If node.val <= min or node.val >= max → false
  3. Return helper(node.left, min, node.val) && helper(node.right, node.val, max)

**Blank Template:**
```kotlin
fun isValidBST(root: TreeNode?): Boolean {
    // TODO: Pass min/max bounds, check node.val is within range
    return false
}
```

---

#### 11. Insert into BST — 🟡 Medium
**LeetCode #701** | Solution: [InsertIntoBST.kt](tree/bst/InsertIntoBST.kt)

- **Hint 1:** Pattern → BST Insert (traverse to leaf)
- **Hint 2:** If val < node.val → go left. If val > node.val → go right. Insert at null position.
- **Hint 3:**
  1. If root == null → return TreeNode(val)
  2. If val < root.`val` → root.left = insert(root.left, val)
  3. Else → root.right = insert(root.right, val)
  4. Return root

**Blank Template:**
```kotlin
fun insertIntoBST(root: TreeNode?, `val`: Int): TreeNode? {
    // TODO: Traverse to null position, create node, return root
    return null
}
```

---

#### 12. Delete Node in BST — 🟡 Medium
**LeetCode #450** | Solution: [DeleteNodeInBST.kt](tree/bst/DeleteNodeInBST.kt)

- **Hint 1:** Pattern → BST Delete (3 cases)
- **Hint 2:** Case 1: no children → remove. Case 2: one child → replace with child. Case 3: two children → replace with in-order successor (min of right subtree), then delete successor.
- **Hint 3:**
  1. Find node to delete (BST search)
  2. If leaf → return null. If one child → return child
  3. If two children → find min in right subtree, copy value, delete min from right subtree

**Blank Template:**
```kotlin
fun deleteNode(root: TreeNode?, key: Int): TreeNode? {
    // TODO: Find node, handle 3 cases (leaf, one child, two children)
    return null
}
```

---

#### 20. Kth Smallest Element in BST — 🟡 Medium
**LeetCode #230** | Solution: [KthSmallestElementInBST.kt](tree/bst/KthSmallestElementInBST.kt)

- **Hint 1:** Pattern → BST In-order Traversal (yields sorted order)
- **Hint 2:** In-order traversal of BST gives elements in ascending order. Stop at kth element.
- **Hint 3:**
  1. In-order: left → node → right
  2. Decrement counter at each node visit
  3. When counter == 0 → record answer

**Blank Template:**
```kotlin
fun kthSmallest(root: TreeNode?, k: Int): Int {
    var count = k
    var result = 0
    // TODO: In-order traversal, decrement count, return when count == 0
    return result
}
```

---

### Pattern 3: Tree — Traversal

---

#### 25. Tree Traversals (In/Pre/Post) — 🟢 Easy
**LeetCode #144/94/145** | Solution: [TreeTraversals.kt](tree/traversal/TreeTraversals.kt)

- **Hint 1:** Pattern → DFS (3 orders)
- **Hint 2:** Pre: node → left → right. In: left → node → right. Post: left → right → node.
- **Hint 3:**
  1. Pre: add node.val, recurse left, recurse right
  2. In: recurse left, add node.val, recurse right
  3. Post: recurse left, recurse right, add node.val

**Blank Template:**
```kotlin
fun inorderTraversal(root: TreeNode?): List<Int> {
    val result = mutableListOf<Int>()
    // TODO: Left → Node → Right
    return result
}
```

---

#### 26. Binary Tree Level Order Traversal — 🟡 Medium
**LeetCode #102** | Solution: [BinaryTreeLevelOrderTraversal.kt](tree/traversal/BinaryTreeLevelOrderTraversal.kt)

- **Hint 1:** Pattern → BFS (Queue)
- **Hint 2:** Use a queue. Process level by level. For each level, dequeue all nodes at that level, enqueue their children.
- **Hint 3:**
  1. Queue with root. While queue not empty:
  2. levelSize = queue.size. For i in 0 until levelSize: poll, add to level list, enqueue children
  3. Add level list to result

**Blank Template:**
```kotlin
fun levelOrder(root: TreeNode?): List<List<Int>> {
    val result = mutableListOf<List<Int>>()
    // TODO: BFS with queue, process level by level
    return result
}
```

---

#### 27. Binary Tree Right Side View — 🟡 Medium
**LeetCode #199** | Solution: [BinaryTreeRightSideView.kt](tree/traversal/BinaryTreeRightSideView.kt)

- **Hint 1:** Pattern → BFS (rightmost node per level)
- **Hint 2:** Level order traversal. For each level, take the last (rightmost) node.
- **Hint 3:**
  1. BFS level by level
  2. For each level, add the last element (queue.peek after processing all at that level)
  3. Or: track level size, add last element of each level

**Blank Template:**
```kotlin
fun rightSideView(root: TreeNode?): List<Int> {
    val result = mutableListOf<Int>()
    // TODO: BFS, take last node of each level
    return result
}
```

---

### Pattern 4: Heap

---

#### 5. Kth Largest Element — 🟡 Medium
**LeetCode #215** | Solution: [KthLargestElement.kt](heap/kth_largest_element/KthLargestElement.kt)

- **Hint 1:** Pattern → Min-Heap of size K
- **Hint 2:** Maintain a min-heap of size K. The top is the Kth largest. Push all elements, pop if size > K.
- **Hint 3:**
  1. Min-heap (PriorityQueue natural order)
  2. For each num: push. If heap.size > k → poll (remove smallest)
  3. Return heap.peek()

**Blank Template:**
```kotlin
fun findKthLargest(nums: IntArray, k: Int): Int {
    val heap = java.util.PriorityQueue<Int>()
    // TODO: Push all, keep size k, return peek
    return 0
}
```

---

#### 6. Top K Frequent Elements — 🟡 Medium
**LeetCode #347** | Solution: [TopKFrequentElements.kt](heap/top_k_frequent_elements/TopKFrequentElements.kt)

- **Hint 1:** Pattern → HashMap (frequency) + Min-Heap of size K
- **Hint 2:** Count frequencies with HashMap. Use min-heap of size K sorted by frequency. Keep top K.
- **Hint 3:**
  1. Build frequency map: num → count
  2. Min-heap by frequency. For each entry: push. If size > k → poll
  3. Extract keys from heap

**Blank Template:**
```kotlin
fun topKFrequent(nums: IntArray, k: Int): IntArray {
    val freq = HashMap<Int, Int>()
    // TODO: Count freq, min-heap by frequency, keep size k
    return intArrayOf()
}
```

---

#### 7. Merge K Sorted Lists — 🔴 Hard
**LeetCode #23** | Solution: [MergeKSortedLists.kt](heap/merge_k_sorted_lists/MergeKSortedLists.kt)

- **Hint 1:** Pattern → Min-Heap (merge heads of all lists)
- **Hint 2:** Push the head of each list into a min-heap. Pop smallest, add to result, push its next node.
- **Hint 3:**
  1. Min-heap comparing by node.val
  2. Push all non-null list heads
  3. While heap not empty: pop, append to result, push popped.next if non-null

**Blank Template:**
```kotlin
fun mergeKLists(lists: Array<ListNode?>): ListNode? {
    val heap = java.util.PriorityQueue<ListNode>(compareBy { it.`val` })
    // TODO: Push all heads, pop smallest, push next, build result
    return null
}
```

---

#### 8. Find Median from Data Stream — 🔴 Hard
**LeetCode #295** | Solution: [FindMedianFromDataStream.kt](heap/find_median_from_data_stream/FindMedianFromDataStream.kt)

- **Hint 1:** Pattern → Two Heaps (Max-Heap for lower half, Min-Heap for upper half)
- **Hint 2:** Max-heap stores smaller half, min-heap stores larger half. Keep sizes balanced (differ by at most 1). Median = top of larger heap or average of both tops.
- **Hint 3:**
  1. addNum: push to max-heap, then move max-heap top to min-heap
  2. If min-heap.size > max-heap.size → move min-heap top to max-heap
  3. findMedian: if sizes equal → average of tops, else → max-heap top

**Blank Template:**
```kotlin
class MedianFinder() {
    private val lower = java.util.PriorityQueue<Int>(reverseOrder()) // max-heap
    private val upper = java.util.PriorityQueue<Int>() // min-heap
    fun addNum(num: Int) { /* TODO: Balance two heaps */ }
    fun findMedian(): Double { /* TODO: Average or top of larger */ return 0.0 }
}
```

---

### Pattern 5: Trie

---

#### 13. Implement Trie (Prefix Tree) — 🟡 Medium
**LeetCode #208** | Solution: [ImplementTrie.kt](trie/implement_trie/ImplementTrie.kt)

- **Hint 1:** Pattern → Trie (children map + isEnd flag)
- **Hint 2:** Each node has a HashMap of char → child node and a boolean isEnd. Insert: traverse/create nodes, mark end. Search: traverse, check isEnd. StartsWith: traverse, return true if path exists.
- **Hint 3:**
  1. TrieNode: children = HashMap<Char, TrieNode>(), isEnd = false
  2. insert: for each char → get or put child. At end → isEnd = true
  3. search: traverse all chars, return node?.isEnd == true
  4. startsWith: traverse all chars, return true if path exists

**Blank Template:**
```kotlin
class Trie() {
    private class Node { val children = HashMap<Char, Node>(); var isEnd = false }
    private val root = Node()
    fun insert(word: String) { /* TODO: Traverse/create, mark isEnd */ }
    fun search(word: String): Boolean { /* TODO: Traverse, check isEnd */ return false }
    fun startsWith(prefix: String): Boolean { /* TODO: Traverse, check path exists */ return false }
}
```

---

#### 14. Word Search II — 🔴 Hard
**LeetCode #212** | Solution: [WordSearchII.kt](trie/word_search_ii/WordSearchII.kt)

- **Hint 1:** Pattern → Trie + Backtracking (DFS on board)
- **Hint 2:** Build a Trie from words. For each cell, DFS in 4 directions. If Trie path matches and isEnd → add word. Mark visited cells.
- **Hint 3:**
  1. Build Trie from words array
  2. For each cell (i, j): DFS with Trie node pointer
  3. If char not in node.children → return. If isEnd → add word to result
  4. Mark board[i][j] as visited ('#'), recurse 4 directions, restore char

**Blank Template:**
```kotlin
fun findWords(board: Array<CharArray>, words: Array<String>): List<String> {
    val result = mutableSetOf<String>()
    // TODO: Build Trie, DFS from each cell, collect matches
    return result.toList()
}
```

---

### Pattern 6: Bit Manipulation

---

#### 15. Single Number — 🟢 Easy
**LeetCode #136** | Solution: [SingleNumber.kt](array/bit_manipulation/single_number/SingleNumber.kt)

- **Hint 1:** Pattern → XOR (a ^ a = 0, a ^ 0 = a)
- **Hint 2:** XOR all numbers. Pairs cancel to 0. The single number remains.
- **Hint 3:**
  1. result = 0
  2. For each num: result ^= num
  3. Return result

**Blank Template:**
```kotlin
fun singleNumber(nums: IntArray): Int {
    var result = 0
    // TODO: XOR all numbers
    return result
}
```

---

#### 16. Single Number II — 🟡 Medium
**LeetCode #137** | Solution: [SingleNumberII.kt](array/bit_manipulation/single_number_ii/SingleNumberII.kt)

- **Hint 1:** Pattern → Bit Counting (mod 3)
- **Hint 2:** Count set bits at each position. If count % 3 != 0 → that bit is set in the answer.
- **Hint 3:**
  1. For each of 32 bit positions: count how many numbers have that bit set
  2. If count % 3 != 0 → set that bit in result
  3. Return result

**Blank Template:**
```kotlin
fun singleNumber(nums: IntArray): Int {
    var result = 0
    // TODO: For each bit position, count set bits, if count % 3 != 0 → set bit
    return result
}
```

---

#### 17. Number of 1 Bits — 🟢 Easy
**LeetCode #191** | Solution: [NumberOf1Bits.kt](array/bit_manipulation/number_of_1_bits/NumberOf1Bits.kt)

- **Hint 1:** Pattern → Bit Manipulation (n & (n-1) trick)
- **Hint 2:** n & (n-1) removes the lowest set bit. Count how many times until n == 0.
- **Hint 3:**
  1. count = 0
  2. While n != 0: n = n and (n - 1), count++
  3. Return count

**Blank Template:**
```kotlin
fun hammingWeight(n: Int): Int {
    var count = 0
    var num = n
    // TODO: n & (n-1) removes lowest set bit, count iterations
    return count
}
```

---

#### 18. Missing Number — 🟢 Easy
**LeetCode #268** | Solution: [MissingNumber.kt](array/bit_manipulation/missing_number/MissingNumber.kt)

- **Hint 1:** Pattern → XOR (index XOR value)
- **Hint 2:** XOR all indices (0 to n) with all values. The missing number remains.
- **Hint 3:**
  1. result = nums.size (n)
  2. For i in 0 until n: result ^= i ^ nums[i]
  3. Return result

**Blank Template:**
```kotlin
fun missingNumber(nums: IntArray): Int {
    var result = nums.size
    // TODO: XOR all indices and values
    return result
}
```

---

#### 21. Sum of Two Integers — 🟡 Medium
**LeetCode #371** | Solution: [SumOfTwoIntegers.kt](array/bit_manipulation/sum_of_two_integers/SumOfTwoIntegers.kt)

- **Hint 1:** Pattern → Bit Manipulation (half-adder)
- **Hint 2:** XOR gives sum without carry. AND gives carry. Repeat until no carry.
- **Hint 3:**
  1. While b != 0: carry = (a & b) << 1
  2. a = a ^ b
  3. b = carry
  4. Return a

**Blank Template:**
```kotlin
fun getSum(a: Int, b: Int): Int {
    var x = a; var y = b
    // TODO: XOR for sum, AND << 1 for carry, repeat until no carry
    return x
}
```

---

#### 22. Power of Two — 🟢 Easy
**LeetCode #231** | Solution: [PowerOfTwo.kt](array/bit_manipulation/power_of_two/PowerOfTwo.kt)

- **Hint 1:** Pattern → Bit Manipulation (single set bit)
- **Hint 2:** A power of 2 has exactly one bit set. n > 0 and (n & (n-1)) == 0.
- **Hint 3:**
  1. If n <= 0 → false
  2. Return (n and (n - 1)) == 0

**Blank Template:**
```kotlin
fun isPowerOfTwo(n: Int): Boolean {
    // TODO: n > 0 and n & (n-1) == 0
    return false
}
```

---

#### 23. Counting Bits — 🟡 Medium
**LeetCode #338** | Solution: [CountingBits.kt](array/bit_manipulation/counting_bits/CountingBits.kt)

- **Hint 1:** Pattern → DP + Bit Manipulation
- **Hint 2:** bits[i] = bits[i >> 1] + (i & 1). The count for i = count for i/2 + the LSB.
- **Hint 3:**
  1. result[0] = 0
  2. For i in 1..n: result[i] = result[i shr 1] + (i and 1)
  3. Return result

**Blank Template:**
```kotlin
fun countBits(n: Int): IntArray {
    val result = IntArray(n + 1)
    // TODO: DP: result[i] = result[i/2] + (i & 1)
    return result
}
```

---

### Pattern 7: String Parsing

---

#### 29. Integer to Roman — 🟡 Medium
**LeetCode #12** | Solution: [IntegerToRoman.kt](array/string_parsing/integer_to_roman/IntegerToRoman.kt)

- **Hint 1:** Pattern → Greedy (largest values first)
- **Hint 2:** Map values to symbols in descending order. Subtract largest possible value, append symbol, repeat.
- **Hint 3:**
  1. Pairs: (1000→M, 900→CM, 500→D, 400→CD, 100→C, 90→XC, 50→L, 40→XL, 10→X, 9→IX, 5→V, 4→IV, 1→I)
  2. For each (value, symbol): while num >= value → append symbol, num -= value
  3. Return result

**Blank Template:**
```kotlin
fun intToRoman(num: Int): String {
    val pairs = listOf(1000 to "M", 900 to "CM", 500 to "D", 400 to "CD", 100 to "C", 90 to "XC", 50 to "L", 40 to "XL", 10 to "X", 9 to "IX", 5 to "V", 4 to "IV", 1 to "I")
    // TODO: Greedily subtract largest values, append symbols
    return ""
}
```

---

#### 30. Longest Palindromic String — 🟡 Medium
**LeetCode #5** | Solution: [LongestPalidromString.kt](array/string_parsing/longest_palidrom_string/LongestPalidromString.kt)

- **Hint 1:** Pattern → Expand Around Center
- **Hint 2:** For each index, expand for odd-length (single center) and even-length (double center) palindromes. Track longest.
- **Hint 3:**
  1. For i in 0 until n: expand(i, i) for odd, expand(i, i+1) for even
  2. expand(left, right): while left >= 0 and right < n and s[left] == s[right] → expand
  3. Track start and maxLength

**Blank Template:**
```kotlin
fun longestPalindrome(s: String): String {
    var start = 0; var maxLen = 0
    // TODO: Expand around each center (odd and even), track longest
    return s.substring(start, start + maxLen)
}
```

---

#### 31. ZigZag Conversion — 🟡 Medium
**LeetCode #6** | Solution: [ZigZagConversionString.kt](array/string_parsing/zig_zag_conversion_string/ZigZagConversionString.kt)

- **Hint 1:** Pattern → Simulation (row tracking)
- **Hint 2:** Simulate going down and up through rows. Use a list of StringBuilders, one per row. Change direction at top and bottom.
- **Hint 3:**
  1. rows = List of StringBuilder, currentRow = 0, direction = +1
  2. For each char: append to rows[currentRow], move currentRow
  3. If currentRow == 0 → direction = +1. If currentRow == numRows-1 → direction = -1
  4. Join all rows

**Blank Template:**
```kotlin
fun convert(s: String, numRows: Int): String {
    if (numRows == 1) return s
    val rows = List(numRows) { StringBuilder() }
    // TODO: Simulate zigzag, track row and direction
    return rows.joinToString("")
}
```

---

### Pattern 8: Set Operations

---

#### 32. Group Anagrams — 🟡 Medium
**LeetCode #49** | Solution: [GroupAnagrams.kt](array/set_operations/group_anagrams/GroupAnagrams.kt)

- **Hint 1:** Pattern → HashMap (sorted string as key)
- **Hint 2:** Two anagrams have the same sorted string. Use sorted string as key, group words.
- **Hint 3:**
  1. Map<String, MutableList<String>>
  2. For each word: key = word.toCharArray().sorted().joinToString("")
  3. map[key].add(word)
  4. Return map.values

**Blank Template:**
```kotlin
fun groupAnagrams(strs: Array<String>): List<List<String>> {
    val map = HashMap<String, MutableList<String>>()
    // TODO: Sort each word as key, group anagrams
    return map.values.toList()
}
```

---

#### 33. Find Difference Between Two Arrays — 🟢 Easy
**LeetCode #2215** | Solution: [FindTheDifferenceBetweenTwoArray.kt](array/set_operations/find_the_difference_between_two_array/FindTheDifferenceBetweenTwoArray.kt)

- **Hint 1:** Pattern → Set Difference
- **Hint 2:** Convert both arrays to sets. Answer[0] = nums1 - nums2, answer[1] = nums2 - nums1.
- **Hint 3:**
  1. set1 = nums1.toSet(), set2 = nums2.toSet()
  2. answer[0] = (set1 - set2).toList()
  3. answer[1] = (set2 - set1).toList()

**Blank Template:**
```kotlin
fun findDifference(nums1: IntArray, nums2: IntArray): List<List<Int>> {
    // TODO: Set difference both ways
    return listOf()
}
```

---

### Pattern 9: Frequency Count

---

#### 34. Count the Number of Pairs — 🟡 Medium
**LeetCode #2006** | Solution: [CountTheNumberOfPairs.kt](array/frequency_count/count_the_number_of_pairs/CountTheNumberOfPairs.kt)

- **Hint 1:** Pattern → Frequency Count (HashMap)
- **Hint 2:** Count frequency of each number. For each number, pairs = freq[num] * freq[num + k] (or freq[num - k] to avoid double counting).
- **Hint 3:**
  1. Build frequency map
  2. For each unique num: if k > 0 → count += freq[num] * freq.getOrDefault(num + k, 0)
  3. If k == 0 → count += freq[num] * (freq[num] - 1) / 2

**Blank Template:**
```kotlin
fun countKDifference(nums: IntArray, k: Int): Int {
    val freq = HashMap<Int, Int>()
    // TODO: Count frequencies, for each num check num + k
    return 0
}
```

---

### Pattern 10: Voting & Floyd's

---

#### 35. Majority Element — 🟢 Easy
**LeetCode #169** | Solution: [MajorityOfElements.kt](array/voting_floyd/majority_of_elements/MajorityOfElements.kt)

- **Hint 1:** Pattern → Boyer-Moore Voting
- **Hint 2:** Maintain a candidate and count. If count == 0 → switch candidate. Same as candidate → count++. Different → count--.
- **Hint 3:**
  1. candidate = 0, count = 0
  2. For each num: if count == 0 → candidate = num
  3. If num == candidate → count++ else → count--
  4. Return candidate

**Blank Template:**
```kotlin
fun majorityElement(nums: IntArray): Int {
    var candidate = 0; var count = 0
    // TODO: Boyer-Moore: switch candidate when count == 0
    return candidate
}
```

---

#### 36. Majority Element II — 🟡 Medium
**LeetCode #229** | Solution: [MajorityElementII.kt](array/voting_floyd/majority_element_ii/MajorityElementII.kt)

- **Hint 1:** Pattern → Boyer-Moore Voting (2 candidates)
- **Hint 2:** At most 2 elements can appear > n/3 times. Track 2 candidates with counts. Verify at end.
- **Hint 3:**
  1. candidate1, candidate2, count1, count2 = 0
  2. For each num: if num == candidate1 → count1++. Else if num == candidate2 → count2++
  3. Else if count1 == 0 → candidate1 = num, count1 = 1. Else if count2 == 0 → candidate2 = num, count2 = 1
  4. Else → count1--, count2--
  5. Verify both candidates appear > n/3 times

**Blank Template:**
```kotlin
fun majorityElement(nums: IntArray): List<Int> {
    // TODO: Two candidates, two counts, verify at end
    return listOf()
}
```

---

#### 37. Find Duplicate Number — 🟡 Medium
**LeetCode #287** | Solution: [FindDupplicateNumber.kt](array/voting_floyd/find_dupplicate_number/FindDupplicateNumber.kt)

- **Hint 1:** Pattern → Floyd's Cycle Detection (Tortoise & Hare)
- **Hint 2:** Treat array as linked list (index → value → next index). The duplicate creates a cycle. Find cycle entry.
- **Hint 3:**
  1. slow = nums[0], fast = nums[nums[0]]
  2. While slow != fast: slow = nums[slow], fast = nums[nums[fast]]
  3. Reset slow to 0. While slow != fast: slow = nums[slow], fast = nums[fast]
  4. Return slow

**Blank Template:**
```kotlin
fun findDuplicate(nums: IntArray): Int {
    var slow = nums[0]; var fast = nums[nums[0]]
    // TODO: Floyd's: find meeting point, then find cycle entry
    return slow
}
```

---

#### 38. Missing Number in Array — 🟢 Easy
**LeetCode #268** | Solution: [MissingNumberInArray.kt](array/voting_floyd/missing_number_in_array/MissingNumberInArray.kt)

- **Hint 1:** Pattern → Math (Sum formula)
- **Hint 2:** Expected sum = n*(n+1)/2. Actual sum = sum of array. Missing = expected - actual.
- **Hint 3:**
  1. n = nums.size
  2. expected = n * (n + 1) / 2
  3. actual = nums.sum()
  4. Return expected - actual

**Blank Template:**
```kotlin
fun missingNumber(nums: IntArray): Int {
    val n = nums.size
    // TODO: Expected sum - actual sum
    return 0
}
```

---

### Pattern 11: Greedy

---

#### 39. Lemonade Change — 🟢 Easy
**LeetCode #860** | Solution: [LemonadeChange.kt](array/greedy/lemonade_change/LemonadeChange.kt)

- **Hint 1:** Pattern → Greedy (track bills)
- **Hint 2:** Track count of $5 and $10 bills. For $5 → keep. For $10 → give $5 change. For $20 → prefer $10 + $5 change (greedy).
- **Hint 3:**
  1. fives = 0, tens = 0
  2. $5: fives++. $10: if fives == 0 → false, fives--, tens++
  3. $20: if tens > 0 and fives > 0 → tens--, fives--. Else if fives >= 3 → fives -= 3. Else → false

**Blank Template:**
```kotlin
fun lemonadeChange(bills: IntArray): Boolean {
    var fives = 0; var tens = 0
    // TODO: Track bills, give change greedily (prefer $10 + $5 for $20)
    return true
}
```

---

#### 40. Jump Game II — 🟡 Medium
**LeetCode #45** | Solution: [JumpGameII.kt](array/greedy/jump_game_ii/JumpGameII.kt)

- **Hint 1:** Pattern → Greedy (BFS-like levels)
- **Hint 2:** Track current jump's reach (currentEnd) and the farthest reachable (farthest). When i reaches currentEnd → jump, set currentEnd = farthest.
- **Hint 3:**
  1. jumps = 0, currentEnd = 0, farthest = 0
  2. For i in 0 until n-1: farthest = max(farthest, i + nums[i])
  3. If i == currentEnd → jumps++, currentEnd = farthest
  4. Return jumps

**Blank Template:**
```kotlin
fun jump(nums: IntArray): Int {
    var jumps = 0; var currentEnd = 0; var farthest = 0
    // TODO: Track farthest, jump when reaching currentEnd
    return jumps
}
```

---

## 📅 Suggested Weekly Practice Plan

| Day | Problems to Practice | Focus |
|-----|---------------------|-------|
| Mon | 2 Easy problems (from "not mastered") | Speed + accuracy |
| Tue | 2 Medium problems (from "not mastered") | Pattern recognition |
| Wed | 1 Hard problem (from "not mastered") | Deep understanding |
| Thu | 2 Easy/Medium (re-attempt failed ones) | Fix weak spots |
| Fri | 2 Medium problems (from "not mastered") | Timed (15 min each) |
| Sat | 1 Hard + 1 Easy review | Mixed difficulty |
| Sun | Rest or re-solve 3 "almost mastered" | Push to mastered |

---

*Back to [Week 3-4 Tracker](Week3-4_Practice_Tracker.md) | [Week 1-2 Tracker](Week1-2_Practice_Tracker.md) | [Revision Notes](Revision_Notes.md) | [Daily DSA Schedule](../interview_schedule/Daily_DSA_Schedule.md) | [Pattern Index](PatternWiseProblems.md)*
