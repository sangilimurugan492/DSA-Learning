package linked_list.single.design_hash_set

import linked_list.single.list_node.ListNode

class MyHashSet() {

    fun add(key: Int) {
//        val newNode = ListNode(key, null)
    }

    fun remove(key: Int) {

    }

    fun contains(key: Int): Boolean {

        return false
    }

}
