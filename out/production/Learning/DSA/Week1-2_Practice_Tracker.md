# 🎯 DSA Guided Practice Tracker — Week 1 & 2

> **Purpose:** Practice problems you've already solved — with progressive hints so you never get fully stuck.
> **Goal:** Wean off looking at solutions. Go from `📖 Read` → `💡 Hints` → `✅ From scratch` → `🏆 Mastered`
> **Problems:** 36 (Week 1 full + Week 2 Days 8–14)
> **Scope:** Week 1 & 2 only. Trackers for later weeks will be created as you progress.

---

## 📖 How to Use This File

### The 4-Day Re-solve Cycle (per problem)

| Day | What to Do | Mark |
|-----|-----------|------|
| **Day 1** | Read the solution, understand it fully. Close the file. | 📖 |
| **Day 2** | Try to solve using only **Hint 1** (pattern name). If stuck, reveal Hint 2. | 💡 |
| **Day 3** | Try to solve using only **Hint 2** (key insight). If stuck, reveal Hint 3. | 💡 |
| **Day 4** | Try to solve **from scratch** — no hints. | ✅ or 🔄 |

- **3 successful from-scratch solves** = 🏆 **Mastered**
- If you fail from scratch, go back to Day 2 and repeat the cycle

### Daily Practice Routine (30 min)
1. Pick **2 problems** from your "not yet mastered" list
2. Follow the cycle for each
3. Update the tracker table below

---

## 📅 Date-Based Preparation Schedule

> Each day maps to the DSA practice slot in your daily checklist. Follow this schedule to stay on track.

### Week 1 — Aug 20 (Thu) to Aug 26 (Wed)

| Date | Day | Problems to Practice | Pattern Focus | Time |
|------|-----|---------------------|---------------|------|
| **Aug 20 (Thu)** | Day 1 | #1 Two Sum, #2 Contains Duplicates | Complement Search + HashSet | 30 min |
| **Aug 21 (Fri)** | Day 2 | #3 Find Smallest & Largest, #4 Plus One | Linear Scan | 30 min |
| **Aug 22 (Sat)** | Day 3 | #5 Single Number, #6 Assign Cookies, #7 Buy & Sell Stock I, #8 Intersection of Two Arrays | Easy Mixed Patterns | 1.5 hr |
| **Aug 23 (Sun)** | Day 4 | #9 Remove Duplicates, #10 Max Average Subarray | Two Pointer In-Place + Sliding Window | 1.5 hr |
| **Aug 24 (Mon)** | Day 5 | #11 Two Sum II, #12 Finding Pairs with Certain Sum | Two Pointer + Complement Search | 30 min |
| **Aug 25 (Tue)** | Day 6 | #13 Buy & Sell Stock II, #14 Jump Game | Greedy | 30 min |
| **Aug 26 (Wed)** | Day 7 | #15 Gas Station, #16 Insert Interval, #17 Interval Groups | Greedy (Intervals) | 1 hr |

### Week 2 — Aug 27 (Thu) to Sep 2 (Wed)

| Date | Day | Problems to Practice | Pattern Focus | Time |
|------|-----|---------------------|---------------|------|
| **Aug 27 (Thu)** | Day 8 | #18 Count Pairs, #19 Finding Fair Pairs, #20 Top K Frequent | Frequency Count | 1 hr |
| **Aug 28 (Fri)** | Day 9 | #21 Search in Rotated Sorted Array, #22 Find Min in Rotated Sorted Array | Binary Search | 30 min |
| **Aug 29 (Sat)** | Day 10 | #23 Single Number II, #24 Maximum Subarray, #25 String to Integer (atoi) | Bit Manip + Kadane's + String Parsing | 1.5 hr |
| **Aug 30 (Sun)** | Day 11 | #26 Product of Array Except Self, #27 Subarray Sum Equals K | Prefix Sum | 1.5 hr |
| **Aug 31 (Mon)** | Day 12 | #28 Longest Substring Without Repeating, #29 Set Matrix Zeroes | Sliding Window + Matrix | 30 min |
| **Sep 1 (Tue)** | Day 13 | #30 Find Duplicate Number, #31 Three Sum, #32 Longest Consecutive Sequence | Floyd's + Two Pointer + HashSet | 1 hr |
| **Sep 2 (Wed)** | Day 14 | #33 Candy, #34 Median of Two Sorted Arrays, #35 Minimum Window Substring, #36 Interval Groups (Hard) | Hard Problems | 1.5 hr |

---

## 📊 Master Tracker

| # | Problem | Pattern | Difficulty | Attempt 1 | Attempt 2 | Attempt 3 | Status |
|---|---------|---------|-----------|-----------|-----------|-----------|--------|
| 1 | Two Sum (Unsorted) | Complement Search | 🟢 | 3/9       | | | [x]    |
| 2 | Contains Duplicates | HashSet Lookup | 🟢 | 3/9       | | | [x]    |
| 3 | Find Smallest & Largest | Linear Scan | 🟢 | 3/9       | | | [x]    |
| 4 | Plus One | Linear Scan | 🟢 | 3/9       | | | [x]    |
| 5 | Single Number | Bit Manipulation | 🟢 |           | | | ⬜      |
| 6 | Assign Cookies | Greedy | 🟢 |           | | | ⬜      |
| 7 | Best Time to Buy & Sell Stock I | Greedy | 🟢 |           | | | ⬜      |
| 8 | Intersection of Two Arrays | Set Operations | 🟢 |           | | | ⬜      |
| 9 | Remove Duplicates (Sorted) | Two Pointer In-Place | 🟢 |           | | | ⬜      |
| 10 | Max Average Subarray | Sliding Window (Fixed) | 🟢 |           | | | ⬜      |
| 11 | Two Sum II (Sorted) | Two Pointer | 🟡 |           | | | ⬜      |
| 12 | Finding Pairs with Certain Sum | Complement Search | 🟡 |           | | | ⬜      |
| 13 | Best Time to Buy & Sell Stock II | Greedy | 🟡 |           | | | ⬜      |
| 14 | Jump Game | Greedy | 🟡 |           | | | ⬜      |
| 15 | Gas Station | Greedy | 🟡 |           | | | ⬜      |
| 16 | Insert Interval | Greedy (Intervals) | 🟡 |           | | | ⬜      |
| 17 | Interval Groups | Greedy (Intervals) | 🟡 |           | | | ⬜      |
| 18 | Count the Number of Pairs | Frequency Count | 🟡 |           | | | ⬜      |
| 19 | Finding Fair Pairs | Frequency Count | 🟡 |           | | | ⬜      |
| 20 | Top K Frequent Elements | Frequency Count / Bucket | 🟡 |           | | | ⬜      |
| 21 | Search in Rotated Sorted Array | Binary Search | 🟡 |           | | | ⬜      |
| 22 | Find Min in Rotated Sorted Array | Binary Search | 🟡 |           | | | ⬜      |
| 23 | Single Number II | Bit Manipulation | 🟡 |           | | | ⬜      |
| 24 | Maximum Subarray | Kadane's | 🟡 |           | | | ⬜      |
| 25 | String to Integer (atoi) | String Parsing | 🟡 |           | | | ⬜      |
| 26 | Product of Array Except Self | Prefix Sum | 🟡 |           | | | ⬜      |
| 27 | Subarray Sum Equals K | Prefix Sum + HashMap | 🟡 |           | | | ⬜      |
| 28 | Longest Substring Without Repeating | Sliding Window (Variable) | 🟡 |           | | | ⬜      |
| 29 | Set Matrix Zeroes | Matrix (In-Place) | 🟡 |           | | | ⬜      |
| 30 | Find Duplicate Number | Floyd's Cycle Detection | 🟡 |           | | | ⬜      |
| 31 | Three Sum | Two Pointer | 🟡 |           | | | ⬜      |
| 32 | Longest Consecutive Sequence | HashSet Lookup | 🟡 |           | | | ⬜      |
| 33 | Candy | Greedy (Two-Pass) | 🔴 |           | | | ⬜      |
| 34 | Median of Two Sorted Arrays | Binary Search (Partition) | 🔴 |           | | | ⬜      |
| 35 | Minimum Window Substring | Sliding Window (Variable) | 🔴 |           | | | ⬜      |
| 36 | Interval Groups (Hard) | Greedy (Intervals) | 🔴 |           | | | ⬜      |

**Legend:** ⬜ Not started | 📖 Read solution | 💡 Used hints | ✅ From scratch | 🏆 Mastered

---

## 🧩 Problems with Progressive Hints

### Pattern 1: Complement Search

---

#### 1. Two Sum (Unsorted) — 🟢 Easy
**LeetCode #1** | Solution: [TwoSumLevelOneWithoutSortedArray.kt](array/complement_search/two_sum_level_one_without_sorted_array/TwoSumLevelOneWithoutSortedArray.kt)

- **Hint 1:** Pattern → Complement Search with HashMap
- **Hint 2:** For each num, check if `target - num` was already seen. HashMap stores `value → index`.
- **Hint 3:**
  1. Create empty HashMap
  2. For each element, compute complement = target - num
  3. If complement in map → return indices
  4. Else add num → index to map

**Blank Template:**
```kotlin
fun twoSum(nums: IntArray, target: Int): IntArray {
    // TODO: HashMap to store value → index
    // TODO: For each num, check if complement exists
    // TODO: Return indices or add to map
    return intArrayOf()
}
```

---

#### 2. Finding Pairs with Certain Sum — 🟡 Medium
**LeetCode #1865** | Solution: [FindingPairsWithCertainSum.kt](array/complement_search/finding_pairs_with_certain_sum/FindingPairsWithCertainSum.kt)

- **Hint 1:** Pattern → Complement Search with frequency map
- **Hint 2:** Maintain frequency map of nums2. For `count(tot)`, for each num1, look up `tot - num1` in map. On `add(index, val)`, update map (decrement old, increment new).
- **Hint 3:**
  1. Build `nums2Map`: frequency count of nums2 values
  2. `count(tot)`: for each num in nums1, add `nums2Map[tot - num]`
  3. `add(index, val)`: decrement old nums2[index] count, update nums2[index], increment new count

**Blank Template:**
```kotlin
class FindSumPairs(val nums1: IntArray, val nums2: IntArray) {
    val nums2Map = mutableMapOf<Int, Int>()
    init { /* TODO: Build frequency map of nums2 */ }

    fun add(index: Int, value: Int) {
        // TODO: Decrement old value count, update nums2[index], increment new count
    }

    fun count(tot: Int): Int {
        // TODO: For each num in nums1, add nums2Map[tot - num]
        return 0
    }
}
```

---

### Pattern 2: Two Pointer

---

#### 3. Two Sum II (Sorted Array) — 🟡 Medium
**LeetCode #167** | Solution: [TwoSumLevelTwoWithSortedArray.kt](array/two_pointer/two_sum_level_two_with_sorted_array/TwoSumLevelTwoWithSortedArray.kt)

- **Hint 1:** Pattern → Two Pointer (sorted array)
- **Hint 2:** Converge from both ends. Sum too small → move left. Sum too big → move right.
- **Hint 3:**
  1. left = 0, right = n-1
  2. If sum < target → left++
  3. If sum > target → right--
  4. If equal → return [left+1, right+1] (1-indexed)

**Blank Template:**
```kotlin
fun twoSumSorted(numbers: IntArray, target: Int): IntArray {
    // TODO: Two pointers from both ends
    // TODO: Compare sum with target, move pointers
    return intArrayOf()
}
```

---

#### 4. Three Sum — 🟡 Medium
**LeetCode #15** | Solution: [ThreeSum.kt](array/two_pointer/three_sum/ThreeSum.kt)

- **Hint 1:** Pattern → Sort + Two Pointer
- **Hint 2:** Fix first element, then use two-pointer for remaining two. Skip duplicates at ALL levels.
- **Hint 3:**
  1. Sort array
  2. For each i: skip if `nums[i] == nums[i-1]`
  3. Set left = i+1, right = n-1
  4. If sum < 0 → left++; if sum > 0 → right--
  5. If sum == 0 → add triplet, then skip duplicate left and right values

**Blank Template:**
```kotlin
fun threeSum(nums: IntArray): List<List<Int>> {
    // TODO: Sort, fix i, two-pointer on rest
    // TODO: Skip duplicates at i, left, and right
    return emptyList()
}
```

---

### Pattern 3: Greedy

---

#### 5. Best Time to Buy & Sell Stock I — 🟢 Easy
**LeetCode #121** | Solution: [BestTimeToBuyAndSellStockI.kt](array/greedy/best_time_to_buy_and_sell_stock_i/BestTimeToBuyAndSellStockI.kt)

- **Hint 1:** Pattern → Greedy, one pass
- **Hint 2:** Track `minPrice` so far. At each day, profit = `price[i] - minPrice`. Update `maxProfit`.
- **Hint 3:**
  1. minPrice = prices[0], maxProfit = 0
  2. For each price: if price < minPrice → update minPrice
  3. profit = price - minPrice → update maxProfit

**Blank Template:**
```kotlin
fun maxProfit(prices: IntArray): Int {
    // TODO: Track minPrice and maxProfit in one pass
    return 0
}
```

---

#### 6. Best Time to Buy & Sell Stock II — 🟡 Medium
**LeetCode #122** | Solution: [BestTimeToBuyAndSellStockII.kt](array/greedy/best_time_to_buy_and_sell_stock_ii/BestTimeToBuyAndSellStockII.kt)

- **Hint 1:** Pattern → Greedy, sum positive differences
- **Hint 2:** Unlimited transactions → sum ALL positive daily differences.
- **Hint 3:**
  1. For each day i: if `prices[i+1] > prices[i]` → add the difference to profit
  2. That's it — every uptick is captured

**Blank Template:**
```kotlin
fun maxProfit(prices: IntArray): Int {
    // TODO: Sum all positive daily differences
    return 0
}
```

---

#### 7. Jump Game — 🟡 Medium
**LeetCode #55** | Solution: [JumpGame.kt](array/greedy/jump_game/JumpGame.kt)

- **Hint 1:** Pattern → Greedy, track farthest
- **Hint 2:** Track `farthest` reachable index. If `i > farthest` → can't reach. If `farthest >= last` → true.
- **Hint 3:**
  1. farthest = 0
  2. For each i: if i > farthest → return false
  3. farthest = max(farthest, i + nums[i])
  4. If farthest >= n-1 → return true

**Blank Template:**
```kotlin
fun canJump(nums: IntArray): Boolean {
    // TODO: Track farthest reachable index
    return false
}
```

---

#### 8. Gas Station — 🟡 Medium
**LeetCode #134** | Solution: [GasStation.kt](array/greedy/gas_station/GasStation.kt)

- **Hint 1:** Pattern → Greedy, one pass
- **Hint 2:** If `totalGas >= totalCost`, solution exists. If `currentTank < 0` at station i, start fresh at `i+1`.
- **Hint 3:**
  1. Track totalTank (overall surplus) and currentTank (running from start)
  2. If currentTank < 0 → reset startStation = i+1, currentTank = 0
  3. If totalTank >= 0 → return startStation, else -1

**Blank Template:**
```kotlin
fun canCompleteCircuit(gas: IntArray, cost: IntArray): Int {
    // TODO: Track totalTank, currentTank, startStation
    return -1
}
```

---

#### 9. Assign Cookies — 🟢 Easy
**LeetCode #455** | Solution: [AssignCookies.kt](array/greedy/assign_cookies/AssignCookies.kt)

- **Hint 1:** Pattern → Greedy, sort both + two pointers
- **Hint 2:** Assign smallest sufficient cookie to least greedy child first.
- **Hint 3:**
  1. Sort both arrays
  2. child = 0, cookie = 0
  3. If cookie >= child greed → child++ (content!)
  4. cookie++ always
  5. Return child count

**Blank Template:**
```kotlin
fun findContentChildren(g: IntArray, s: IntArray): Int {
    // TODO: Sort both, greedy match smallest cookie to least greedy
    return 0
}
```

---

#### 10. Candy — 🔴 Hard
**LeetCode #135** | Solution: [Candy.kt](array/greedy/candy/Candy.kt)

- **Hint 1:** Pattern → Greedy, two-pass (L→R then R→L)
- **Hint 2:** L→R ensures higher rating than left neighbor gets more. R→L ensures higher than right neighbor. Take max of both passes.
- **Hint 3:**
  1. Initialize all candies = 1
  2. Pass 1 (L→R): if ratings[i] > ratings[i-1] → candies[i] = candies[i-1] + 1
  3. Pass 2 (R→L): if ratings[i] > ratings[i+1] → candies[i] = max(candies[i], candies[i+1] + 1)
  4. Return sum

**Blank Template:**
```kotlin
fun candy(ratings: IntArray): Int {
    // TODO: Two passes L→R and R→L, take max
    return 0
}
```

---

#### 11. Insert Interval — 🟡 Medium
**LeetCode #57** | Solution: [InsertInterval.kt](array/greedy/insert_interval/InsertInterval.kt)

- **Hint 1:** Pattern → Greedy, three phases
- **Hint 2:** (1) Add all intervals ending before newInterval, (2) merge overlapping, (3) add remaining.
- **Hint 3:**
  1. While intervals[i].end < newInterval.start → add to result, i++
  2. While intervals[i].start <= newInterval.end → merge (update newInterval start/end), i++
  3. Add merged newInterval
  4. Add remaining intervals

**Blank Template:**
```kotlin
fun insert(intervals: Array<IntArray>, newInterval: IntArray): Array<IntArray> {
    // TODO: Phase 1: add before, Phase 2: merge, Phase 3: add remaining
    return emptyArray()
}
```

---

#### 12. Divide Intervals Into Minimum Number of Groups — 🟡 Medium
**LeetCode #2406** | Solution: [IntervalGroups.kt](array/greedy/interval_groups/IntervalGroups.kt)

- **Hint 1:** Pattern → Greedy, same as Meeting Rooms II (min-heap)
- **Hint 2:** Max overlap at any time = min groups needed. Sort by start, heap tracks end times.
- **Hint 3:**
  1. Sort intervals by start time
  2. Min-heap of end times
  3. For each interval: if heap.peek() < interval.start → poll (reuse group)
  4. Push interval.end to heap
  5. Return heap.size

**Blank Template:**
```kotlin
fun minGroups(intervals: Array<IntArray>): Int {
    // TODO: Sort by start, min-heap of end times, reuse ended groups
    return 0
}
```

---

### Pattern 4: Frequency Count

---

#### 13. Count the Number of Pairs — 🟡 Medium
**LeetCode #2006** | Solution: [CountTheNumberOfPairs.kt](array/frequency_count/count_the_number_of_pairs/CountTheNumberOfPairs.kt)

- **Hint 1:** Pattern → Frequency Count with HashMap
- **Hint 2:** For each element, check if `num + k` or `num - k` exists in the frequency map.
- **Hint 3:**
  1. Build frequency map as you go
  2. For each num: count += map[num + k] + map[num - k]
  3. Then add num to map

**Blank Template:**
```kotlin
fun countNumberOfPairs(nums: IntArray, k: Int): Int {
    // TODO: HashMap, for each num check num+k and num-k
    return 0
}
```

---

#### 14. Finding Fair Pairs — 🟡 Medium
**LeetCode #2563** | Solution: [FindingFairPairs.kt](array/frequency_count/finding_fair_pairs/FindingFairPairs.kt)

- **Hint 1:** Pattern → Sort + Two Pointer counting
- **Hint 2:** Count pairs with sum ≤ upper, subtract pairs with sum < lower. `countLessEqual(target)` uses two pointers.
- **Hint 3:**
  1. Sort array
  2. `countLessEqual(upper) - countLessEqual(lower - 1)`
  3. In countLessEqual: if `nums[left] + nums[right] <= target` → count += (right - left), left++
  4. Else right--

**Blank Template:**
```kotlin
fun findingFairPairs(nums: IntArray, lower: Int, upper: Int): Long {
    // TODO: Sort, countLessEqual(upper) - countLessEqual(lower-1)
    return 0
}
```

---

#### 15. Top K Frequent Elements — 🟡 Medium
**LeetCode #347** | Solution: [TopKFrequentElements.kt](array/frequency_count/top_k_frequent_elements/TopKFrequentElements.kt)

- **Hint 1:** Pattern → Frequency Count + Bucket Sort
- **Hint 2:** Bucket sort — index = frequency, value = list of numbers. Traverse from highest bucket.
- **Hint 3:**
  1. Build frequency map
  2. Create buckets array of size n+1, each bucket = list of numbers
  3. For each (num, count) → buckets[count].add(num)
  4. Traverse from highest bucket down, collect k numbers

**Blank Template:**
```kotlin
fun topKFrequent(nums: IntArray, k: Int): IntArray {
    // TODO: Frequency map → bucket sort → traverse from top
    return intArrayOf()
}
```

---

### Pattern 5: Binary Search

---

#### 16. Search in Rotated Sorted Array — 🟡 Medium
**LeetCode #33** | Solution: [SearchInRotatedSortedArray.kt](array/binary_search/search_in_rotated_sorted_array/SearchInRotatedSortedArray.kt)

- **Hint 1:** Pattern → Binary Search
- **Hint 2:** At any mid, one half is ALWAYS sorted. Find which half is sorted, check if target is in it.
- **Hint 3:**
  1. If nums[left] <= nums[mid] → left half sorted
  2. If target in [nums[left], nums[mid]) → go left, else go right
  3. Else right half sorted
  4. If target in (nums[mid], nums[right]] → go right, else go left

**Blank Template:**
```kotlin
fun searchRotated(nums: IntArray, target: Int): Int {
    // TODO: Binary search, find sorted half, check if target in range
    return -1
}
```

---

#### 17. Find Minimum in Rotated Sorted Array — 🟡 Medium
**LeetCode #153** | Solution: [FindMinimumInRotatedSortedArray.kt](array/binary_search/find_minimum_in_rotated_sorted_array/FindMinimumInRotatedSortedArray.kt)

- **Hint 1:** Pattern → Binary Search
- **Hint 2:** Compare `nums[mid]` with `nums[right]`. If `nums[mid] > nums[right]` → min is in right half.
- **Hint 3:**
  1. left = 0, right = n-1
  2. While left < right: mid = (left + right) / 2
  3. If nums[mid] > nums[right] → left = mid + 1
  4. Else → right = mid
  5. Return nums[left]

**Blank Template:**
```kotlin
fun findMin(nums: IntArray): Int {
    // TODO: Binary search, compare mid with right
    return 0
}
```

---

#### 18. Median of Two Sorted Arrays — 🔴 Hard
**LeetCode #4** | Solution: [MedianOfTwoSortedSubArray.kt](array/binary_search/median_of_two_sorted_arrays/MedianOfTwoSortedSubArray.kt)

- **Hint 1:** Pattern → Binary Search (Partition)
- **Hint 2:** Binary search on smaller array to find partition. `partitionY = (m + n + 1) / 2 - partitionX`. Valid when `maxLeftX ≤ minRightY && maxLeftY ≤ minRightX`.
- **Hint 3:**
  1. Ensure nums1 is smaller array
  2. Binary search on partitionX in [0, m]
  3. partitionY = (m + n + 1) / 2 - partitionX
  4. Use MIN_VALUE / MAX_VALUE for edge partitions
  5. If valid → even: (max(left) + min(right)) / 2, odd: max(left)
  6. If maxLeftX > minRightY → move left, else move right

**Blank Template:**
```kotlin
fun findMedianSortedArrays(nums1: IntArray, nums2: IntArray): Double {
    // TODO: Binary search on smaller array, partition both, check validity
    return 0.0
}
```

---

### Pattern 6: Bit Manipulation

---

#### 19. Single Number — 🟢 Easy
**LeetCode #136** | Solution: [SingleNumber.kt](array/bit_manipulation/single_number/SingleNumber.kt)

- **Hint 1:** Pattern → Bit Manipulation (XOR)
- **Hint 2:** XOR all numbers. `a ^ a = 0`, `a ^ 0 = a`. Pairs cancel out.
- **Hint 3:**
  1. result = 0
  2. For each num: result = result xor num
  3. Return result

**Blank Template:**
```kotlin
fun singleNumber(nums: IntArray): Int {
    // TODO: XOR all numbers
    return 0
}
```

---

#### 20. Single Number II — 🟡 Medium
**LeetCode #137** | Solution: [SingleNumberII.kt](array/bit_manipulation/single_number_ii/SingleNumberII.kt)

- **Hint 1:** Pattern → Bit Manipulation (two counters)
- **Hint 2:** Use `ones` and `twos` to track bits appearing 1 and 2 times (mod 3). After 3 appearances, bits reset.
- **Hint 3:**
  1. ones = 0, twos = 0
  2. For each num:
     - ones = (ones ^ num) & ~twos
     - twos = (twos ^ num) & ~ones
  3. Return ones

**Blank Template:**
```kotlin
fun singleNumberII(nums: IntArray): Int {
    // TODO: Two variables ones and twos, update with XOR and mask
    return 0
}
```

---

### Pattern 7: Linear Scan

---

#### 21. Find Smallest & Largest — 🟢 Easy
Solution: [FindSmallestOrLargest.kt](array/linear_scan/find_smallest_or_largest/FindSmallestOrLargest.kt)

- **Hint 1:** Pattern → Linear Scan, one pass
- **Hint 2:** Track `smallest` and `largest` variables. Update at each element.
- **Hint 3:**
  1. smallest = arr[0], largest = arr[0]
  2. For each element: if < smallest → update, if > largest → update

**Blank Template:**
```kotlin
fun findSmallestOrLargest(value: Array<Int>) {
    // TODO: Track smallest and largest in one pass
}
```

---

#### 22. Plus One — 🟢 Easy
**LeetCode #66** | Solution: [PlusOne.kt](array/linear_scan/plus_one/PlusOne.kt)

- **Hint 1:** Pattern → Linear Scan, right to left with carry
- **Hint 2:** Start from rightmost digit, add carry=1. If digit becomes 10, set to 0 and carry continues.
- **Hint 3:**
  1. carry = 1
  2. For i from last to first: sum = digits[i] + carry, digits[i] = sum % 10, carry = sum / 10
  3. If carry == 0 → return early
  4. If carry remains → create new array with 1 at front

**Blank Template:**
```kotlin
fun plusOne(digits: IntArray): IntArray {
    // TODO: Right to left with carry, handle all-9s case
    return intArrayOf()
}
```

---

### Pattern 8: Kadane's Algorithm

---

#### 23. Maximum Subarray — 🟡 Medium
**LeetCode #53** | Solution: [MaximumSubArray.kt](array/kadane_algorithm/maximum_subarray/MaximumSubArray.kt)

- **Hint 1:** Pattern → Kadane's Algorithm
- **Hint 2:** At each index, decide: extend or start fresh? `localMax = max(nums[i], localMax + nums[i])`.
- **Hint 3:**
  1. localMax = nums[0], globalMax = nums[0]
  2. For each i from 1: localMax = max(nums[i], localMax + nums[i])
  3. globalMax = max(globalMax, localMax)
  4. Return globalMax

**Blank Template:**
```kotlin
fun maxSubArray(nums: IntArray): Int {
    // TODO: Track localMax (extend or start fresh) and globalMax
    return 0
}
```

---

### Pattern 9: HashSet Lookup

---

#### 24. Contains Duplicates — 🟢 Easy
**LeetCode #217** | Solution: [ContainsDuplicates.kt](array/hashset_lookup/contains_duplicates/ContainsDuplicates.kt)

- **Hint 1:** Pattern → HashSet Lookup
- **Hint 2:** Add each element to HashSet. If already exists → duplicate found.
- **Hint 3:**
  1. Create empty HashSet
  2. For each num: if in set → return true, else add to set
  3. Return false

**Blank Template:**
```kotlin
fun containsDuplicate(nums: IntArray): Boolean {
    // TODO: HashSet, check existence as you go
    return false
}
```

---

#### 25. Longest Consecutive Sequence — 🟡 Medium
**LeLeetCode #128** | Solution: [LongestConsecutiveSequence.kt](array/hashset_lookup/longest_consecutive_sequence/LongestConsecutiveSequence.kt)

- **Hint 1:** Pattern → HashSet Lookup
- **Hint 2:** Only START counting from sequence beginnings — a number is a start if `num - 1` is NOT in the set.
- **Hint 3:**
  1. Add all nums to a set
  2. For each num in set: if num-1 NOT in set → it's a start
  3. From start, count consecutive: while num+1 in set → len++
  4. Track maxLen

**Blank Template:**
```kotlin
fun longestConsecutive(nums: IntArray): Int {
    // TODO: Add to set, only count from sequence starts (num-1 not in set)
    return 0
}
```

---

### Pattern 10: Set Operations

---

#### 26. Intersection of Two Arrays — 🟢 Easy
**LeetCode #349** | Solution: [IntersectionOfTwoArrays.kt](array/set_operations/intersection_of_two_arrays/IntersectionOfTwoArrays.kt)

- **Hint 1:** Pattern → Set Operations
- **Hint 2:** Add all nums1 to a HashSet. For each num in nums2, if in set → add to result and remove from set (uniqueness).
- **Hint 3:**
  1. Add nums1 to set
  2. For each num in nums2: if set.remove(num) → add to result
  3. Return result

**Blank Template:**
```kotlin
fun intersection(nums1: IntArray, nums2: IntArray): IntArray {
    // TODO: Add nums1 to set, check nums2 against set, remove for uniqueness
    return intArrayOf()
}
```

---

### Pattern 11: String Parsing

---

#### 27. String to Integer (atoi) — 🟡 Medium
**LeetCode #8** | Solution: [StringToInteger.kt](array/string_parsing/string_to_integer/StringToInteger.kt)

- **Hint 1:** Pattern → String Parsing
- **Hint 2:** Trim whitespace, handle optional sign, parse digits until non-digit. Clamp to Int range on overflow.
- **Hint 3:**
  1. Trim string
  2. Check for '-' or '+' at index 0
  3. Parse consecutive digits
  4. If empty or just sign → return 0
  5. Clamp to Int.MIN_VALUE / Int.MAX_VALUE

**Blank Template:**
```kotlin
fun myAtoi(s: String): Int {
    // TODO: Trim, handle sign, parse digits, clamp overflow
    return 0
}
```

---

### Pattern 12: Prefix Sum

---

#### 28. Product of Array Except Self — 🟡 Medium
**LeetCode #238** | Solution: [ProductOfArrayExceptSelf.kt](array/prefix_sum/product_of_array_except_self/ProductOfArrayExceptSelf.kt)

- **Hint 1:** Pattern → Prefix Sum (prefix × suffix products)
- **Hint 2:** `result[i] = prefixProduct[i] × suffixProduct[i]`. Two passes: L→R builds prefix, R→L multiplies by running suffix. No division!
- **Hint 3:**
  1. result[0] = 1
  2. Pass 1 (L→R): result[i] = result[i-1] * nums[i-1]
  3. Pass 2 (R→L): suffix = 1, result[i] *= suffix, suffix *= nums[i]
  4. Return result

**Blank Template:**
```kotlin
fun productExceptSelf(nums: IntArray): IntArray {
    // TODO: Pass 1: prefix products, Pass 2: multiply by running suffix
    return intArrayOf()
}
```

---

#### 29. Subarray Sum Equals K — 🟡 Medium
**LeetCode #560** | Solution: [SubarraySumEqualsK.kt](array/prefix_sum/subarray_sum_equals_k/SubarraySumEqualsK.kt)

- **Hint 1:** Pattern → Prefix Sum + HashMap
- **Hint 2:** If `prefix[j] - prefix[i] = k`, subarray `[i+1..j]` sums to k. Count how many previous prefix sums equal `prefix[j] - k`.
- **Hint 3:**
  1. HashMap: prefixSum → count, init with {0 → 1}
  2. For each num: prefixSum += num
  3. count += map[prefixSum - k]
  4. map[prefixSum]++

**Blank Template:**
```kotlin
fun subarraySum(nums: IntArray, k: Int): Int {
    // TODO: Prefix sum + HashMap, init {0:1}, count prefixSum - k
    return 0
}
```

---

### Pattern 13: Sliding Window

---

#### 30. Max Average Subarray — 🟢 Easy
**LeetCode #643** | Solution: [MaximumAverageSubArray.kt](array/sliding_window/maximum_average_sub_array/MaximumAverageSubArray.kt)

- **Hint 1:** Pattern → Sliding Window (Fixed K)
- **Hint 2:** Fixed window of size k. Add new element on right, remove old element on left.
- **Hint 3:**
  1. Compute sum of first k elements
  2. For each i from k: sum += nums[i] - nums[i-k]
  3. Track max average

**Blank Template:**
```kotlin
fun findMaxAverage(nums: IntArray, k: Int): Double {
    // TODO: Fixed window, add right, remove left
    return 0.0
}
```

---

#### 31. Longest Substring Without Repeating Characters — 🟡 Medium
**LeetCode #3** | Solution: [LongestSubstringWithoutRepeatingCharacters.kt](array/sliding_window/longest_substring_without_repeating_characters/LongestSubstringWithoutRepeatingCharacters.kt)

- **Hint 1:** Pattern → Sliding Window (Variable)
- **Hint 2:** HashMap stores `char → lastSeenIndex`. When duplicate found AND its last index ≥ left, jump `left` to `lastSeen[char] + 1`.
- **Hint 3:**
  1. HashMap: char → last index
  2. For each right: if char in map and map[char] >= left → left = map[char] + 1
  3. Update map[char] = right
  4. Track maxLen = max(maxLen, right - left + 1)

**Blank Template:**
```kotlin
fun lengthOfLongestSubstring(s: String): Int {
    // TODO: HashMap char→index, jump left on duplicate
    return 0
}
```

---

#### 32. Minimum Window Substring — 🔴 Hard
**LeetCode #76** | Solution: [MinimumWindowSubstring.kt](array/sliding_window/minimum_window_substring/MinimumWindowSubstring.kt)

- **Hint 1:** Pattern → Sliding Window (Variable)
- **Hint 2:** Expand right until window is valid (has all chars of t), then shrink left to minimize. Track `formed` vs `required`.
- **Hint 3:**
  1. Build `need` map from t (char → count)
  2. `have` map, `formed` = 0, `required` = need.size
  3. Expand right: add char to have, if have[char] == need[char] → formed++
  4. While formed == required: update min, shrink left (decrement have, update formed)

**Blank Template:**
```kotlin
fun minWindow(s: String, t: String): String {
    // TODO: need map, have map, formed counter, expand right, shrink left
    return ""
}
```

---

### Pattern 14: Matrix

---

#### 33. Set Matrix Zeroes — 🟡 Medium
**LeetCode #73** | Solution: [SetMatrixZeroes.kt](array/matrix/set_matrix_zeroes/SetMatrixZeroes.kt)

- **Hint 1:** Pattern → Matrix (In-Place)
- **Hint 2:** Use first row and first column as markers. Two booleans track if first row/col themselves need zeroing.
- **Hint 3:**
  1. Check if first row/col have zeros → set booleans
  2. Mark zeros in first row/col for rest of matrix
  3. Set zeros based on markers (skip first row/col)
  4. Zero first row/col last if booleans set

**Blank Template:**
```kotlin
fun setZeroes(matrix: Array<IntArray>) {
    // TODO: Use first row/col as markers, 2 booleans for first row/col
}
```

---

### Pattern 15: Two Pointer In-Place

---

#### 34. Remove Duplicates from Sorted Array — 🟢 Easy
**LeetCode #26** | Solution: [RemoveDuplicateFromSortedArray.kt](array/two_pointer_inplace/remove_duplicate_from_sorted_array/RemoveDuplicateFromSortedArray.kt)

- **Hint 1:** Pattern → Two Pointer In-Place (slow/fast)
- **Hint 2:** Slow pointer tracks position for next unique element. Fast pointer scans ahead.
- **Hint 3:**
  1. count = 1 (first element always unique)
  2. For i from 1: if nums[i] != nums[count-1] → nums[count] = nums[i], count++
  3. Return count

**Blank Template:**
```kotlin
fun removeDuplicates(nums: Array<Int>): Int {
    // TODO: Slow/fast pointer, overwrite in-place
    return 0
}
```

---

### Pattern 16: Voting & Floyd's

---

#### 35. Find Duplicate Number — 🟡 Medium
**LeetCode #287** | Solution: [FindDupplicateNumber.kt](array/voting_floyd/find_dupplicate_number/FindDupplicateNumber.kt)

- **Hint 1:** Pattern → Floyd's Cycle Detection
- **Hint 2:** Treat array as linked list (index → nums[index]). Cycle exists. Phase 1: find meeting point. Phase 2: find cycle entrance (the duplicate).
- **Hint 3:**
  1. slow = nums[0], fast = nums[0]
  2. Phase 1: slow = nums[slow], fast = nums[nums[fast]] until they meet
  3. Phase 2: slow = nums[0], move both one step until they meet → that's the duplicate

**Blank Template:**
```kotlin
fun findDuplicate(nums: IntArray): Int {
    // TODO: Floyd's — Phase 1: find meeting point, Phase 2: find cycle entrance
    return 0
}
```

---

## 📅 Suggested Weekly Practice Plan

| Day | Problems to Practice | Focus |
|-----|---------------------|-------|
| Mon | 2 Easy problems (from "not mastered") | Speed + accuracy |
| Tue | 2 Medium problems (from "not mastered") | Pattern recognition |
| Wed | 1 Hard problem (from "not mastered") | Deep understanding |
| Thu | 2 Easy/Medium (re-attempt failed ones) | Fix weak spots |
| Fri | 2 Medium problems (from "not mastered") | Timed (15 min each) |
| Sat | 1 Hard + 1 Easy review | Mixed difficulty |
| Sun | Rest or re-solve 3 "almost mastered" | Push to mastered |

---

*Back to [Revision Notes](Revision_Notes.md) | [Daily DSA Schedule](../interview_schedule/Daily_DSA_Schedule.md) | [Pattern Index](PatternWiseProblems.md)*
